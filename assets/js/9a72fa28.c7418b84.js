"use strict";(globalThis.webpackChunkmy_website=globalThis.webpackChunkmy_website||[]).push([[331],{3380(e,t,o){o.r(t),o.d(t,{assets:()=>h,contentTitle:()=>r,default:()=>c,frontMatter:()=>i,metadata:()=>n,toc:()=>l});const n=JSON.parse('{"id":"DevOps/Version 1/Beginner_Layer_CI","title":"Beginner_Layer_CI","description":"This beginner\'s DevOps course is your first\xa0 step toward a DevOps engineering role. It is taught by the CEO and co founder of LayerCI. The goal of this course will be for regular\xa0 developers and regular engineering practitioners to learn fundamental DevOps concepts so that\xa0 they can go towards the DevOps engineering role. We\'ll also be talking about DevOps\xa0 broadly in the introduction. But beyond that will primarily be talking\xa0 about the engineering side of things. DevOps is a methodology that helps\xa0 engineering teams build products","source":"@site/docs/DevOps/Version 1/Beginner_Layer_CI.md","sourceDirName":"DevOps/Version 1","slug":"/DevOps/Version 1/Beginner_Layer_CI","permalink":"/Backend-Notes/docs/DevOps/Version 1/Beginner_Layer_CI","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{},"sidebar":"devopsV1","next":{"title":"DevOps - Version 1","permalink":"/Backend-Notes/docs/DevOps/Version 1/intro"}}');var a=o(4848),s=o(8453);const i={},r=void 0,h={},l=[];function d(e){const t={p:"p",...(0,s.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(t.p,{children:"This beginner's DevOps course is your first\xa0 step toward a DevOps engineering role. It is taught by the CEO and co founder of LayerCI. The goal of this course will be for regular\xa0 developers and regular engineering practitioners to learn fundamental DevOps concepts so that\xa0 they can go towards the DevOps engineering role. We'll also be talking about DevOps\xa0 broadly in the introduction. But beyond that will primarily be talking\xa0 about the engineering side of things. DevOps is a methodology that helps\xa0 engineering teams build products"}),"\n",(0,a.jsx)(t.p,{children:"better by continuously integrating user feedback. And if you google DevOps, and you look for\xa0 pictures, you'll often see ones like this, it really helps understand how DevOps\xa0 is different than the traditional way of thinking about software developed.\xa0 Back in the day, software was developed, much like things would be developed in a\xa0 factory. So any input would be programming, and then the output, you'd have a product that\xa0 you could put on a CD, and you'd sell it to users. But since the advent of the Internet,\xa0 and have continuously updatable software,"}),"\n",(0,a.jsx)(t.p,{children:"it's become really easy to launch things and get\xa0 user feedback and integrate that into the current product instead of making a new version of the\xa0 product. So websites like Facebook, continuously upgrade instead of requiring you to buy, you\xa0 know, a new version of Facebook, unlike, you know, old games, like some city would require you to buy\xa0 a new version of SimCity. And that idea is really formalized by DevOps, the sections are planning,\xa0 where you take a set of features that you want to build. And you work with your team to make some\xa0 specifications for those features might look like,"}),"\n",(0,a.jsx)(t.p,{children:"you code them. So developers on your team will\xa0 build out these features so that they can be released. And of course, they're built. So for\xa0 a website, you might take the source code and bundle it into JavaScript that a user's browser\xa0 could run. For a video game, you might make releases for various different versions that\xa0 run on Linux and versions that run on Windows and versions that run in the browser. So you\xa0 take these built artifacts, and you test them. So testing is both automatic and manual.\xa0 Automatic testing is usually colloquially known"}),"\n",(0,a.jsx)(t.p,{children:"as continuous integration. And manual testing\xa0 is colloquially known as quality assurance, QA. And then after it's tested, and you know, the\xa0 stakeholders have all given their feedback, it's released. And continuous deployment\xa0 strategies, releasing and deploying all happens automatically after a change is known to be good.\xa0 There's a lot of automation that can be done here. In larger teams. There's, you know, popular tools\xa0 like Spinnaker by netflix that we'll talk about in later talks. But the core idea is you want to\xa0 take the software, and you want to send it to your"}),"\n",(0,a.jsx)(t.p,{children:"users in a way that they don't notice if there's\xa0 problems. So if there's an experimental UI change, you might show it to a few percentage of\xa0 users and get their feedback before you show it broadly. Again, a company like Facebook,\xa0 which has billions of users, even if 1% of their users complain, they'll get hundreds of millions\xa0 of emails, the release is built and deployed. So deploying means it's released to your users for a\xa0 website, it would mean it's publicly accessible on"}),"\n",(0,a.jsx)(t.p,{children:"the internet. For a CD ROM, you know, you bundle\xa0 your things onto a CD and you distribute that for a mobile release, you'd build the artifact\xa0 and you submit it to the App Store. And then the App Store would review it and then publish\xa0 a new update that your users could download. And then you operate it. Operating\xa0 is primarily things like scaling, making sure that enough resources exist for\xa0 the load, adding more servers as required. configuring things dealing with\xa0 architectural problems, essentially,"}),"\n",(0,a.jsx)(t.p,{children:"on monitoring. So as your users use your software,\xa0 and especially as they submit things, and start jobs and create posts on your forums, you want\xa0 to make sure that those posts are all healthy. And then finally, you take all this feedback,\xa0 and you put it back to the planning stage. So the planning stage takes all the user feedback\xa0 takes all of the things that the operations and deployments, teams learned about deploying\xa0 and scaling the product. And then use that to build out new features, solve bugs, and make\xa0 new versions of the back end and new versions of"}),"\n",(0,a.jsx)(t.p,{children:"the architecture. And then just continues in the\xa0 cycle. And this is what people mean when they say, our company uses DevOps, or our company is\xa0 tech forward, or our company is digitally transformed. They mean that instead of taking a\xa0 set of requirements and building one artifact, which is then shipped, it's a continuous cycle\xa0 of taking feedback. You know, in these two weeks Scrum cycles, usually, and producing software\xa0 that users actually want to use that they've had some say in producing DevOps engineering is\xa0 another common part of DevOps. So beyond just"}),"\n",(0,a.jsx)(t.p,{children:"the methodology, which is something that maybe\xa0 the technical leaders and CEO would care about, there's a subfield of DevOps engineering,\xa0 and this is usually what engineers mean. You know when they say DevOps, and\xa0 that's usually what job postings mean when they say DevOps. So if a job\xa0 posting is asking for a DevOps engineer, you know, they're not asking for\xa0 someone that can plan and deploy code. They're mostly asking for someone that\xa0 can build a test, release, deploy and monitor."}),"\n",(0,a.jsx)(t.p,{children:"So the three pillars of DevOps engineering, our\xa0 pull request automation, deployment, automation, and application performance management. And we'll\xa0 get into specifics about those. But the idea is, pull request automation helps developers build\xa0 things faster, and helps them understand whether their proposed change is good, faster. Deployment\xa0 automation, helps you deploy your code in a way that users don't complain. Again, Facebook has\xa0 lots of deployment automation, because if they just threw their code out into the void,\xa0 every time a developer made a change,"}),"\n",(0,a.jsx)(t.p,{children:"there'd be hundreds of millions of complaints. And\xa0 application performance management is automation around making sure that things are healthy. So\xa0 automatically detecting downtime, automatically waking someone up, if you know, the site goes\xa0 down overnight, automatically rolling back things if there's a problem. And we'll get into\xa0 the specifics of all of these in future talks. The first pillar, which I mentioned\xa0 was pull request automation has primarily to do with the developer feedback\xa0 cycle. So developers share work with each other"}),"\n",(0,a.jsx)(t.p,{children:"by proposing these atomic sets of changes\xa0 called pull requests. And by atomic I mean, they're full featured on their own,\xa0 they don't require other things to run. First, it's like if a developer proposes a pull\xa0 request, they should be expecting that that change is good. And as far as they can tell, the change\xa0 fulfills some business requirements. And then what they have to do is get through some gates.\xa0 So organizations and pull request automation, their goal is to make sure that developers can\xa0 tell very quickly whether their change is good"}),"\n",(0,a.jsx)(t.p,{children:"or not. So for example, if you're working on a\xa0 website, and the developer proposes a change that adds a typo, that's something that can easily\xa0 be automatically detected. And if you set up a typo gate that says no change may go in if it\xa0 contains a typo, that would be an easy way to make sure that developers get automatic feedback about\xa0 their changes. People say pull requests, you know, as of 2021, usually they mean Git. So Git is\xa0 a technology originally popularized by Linux, and it helps developers make these sorts of\xa0 changes and share them with each other. a pull"}),"\n",(0,a.jsx)(t.p,{children:"request is usually reviewed by at least one other\xa0 programmer and something called a code review, where the other programmer will tell you about\xa0 code style will tell the proposing programmer, whether there's architectural problems, scaling\xa0 problems, subjective things that can't easily be automated. But that process of review can also be\xa0 greatly facilitated by a DevOps technology stack. And DevOps automation can help with things like\xa0 ephemeral environments, and from linting. And from all of these other automations that we'll\xa0 get into, after the code review has been done."}),"\n",(0,a.jsx)(t.p,{children:"Usually, an engineering manager or product manager\xa0 in charge of the functionality being proposed, will get feedback. So if you create a new button,\xa0 on a website, you'd like the designer that designed the button, and you'd like the product\xa0 manager that requested the button be created. Both give feedback because if the button is\xa0 phrased poorly, if it's placed poorly, if it's not mobile, responsive, those are all problems that\xa0 would require another merge request. So it would be great if the original merge request fulfilled\xa0 all of the requirements, the first time it was"}),"\n",(0,a.jsx)(t.p,{children:"proposed. And so usually, non technical people\xa0 will give feedback on pull requests as necessary. So what can be automated for a DevOps engineer, you can automate things like automated test running, per change ephemeral\xa0 environments, automated security scanning, notifications through reviewers, getting the\xa0 right people to review it at the right time. And the end goal of all this automation is that\xa0 a developer should be able to propose a change and get it merged the same day they propose the\xa0 change. That's a huge organizational benefit,"}),"\n",(0,a.jsx)(t.p,{children:"because it means that critical bugs can be very\xa0 quickly fixed and merged and deployed without needing a special process. And it also means that\xa0 developers aren't bogged down in bureaucracy, they can propose changes once they get through\xa0 all the gates, the change will be deployed, there isn't additional special gates that\xa0 they need to discover. So for example, if the proper gates and automations have been set up,\xa0 a developer should be able to change a web page without having to ask everyone in the company.\xa0 Whether this web page is used in certain workflows"}),"\n",(0,a.jsx)(t.p,{children:"or not. By virtue of passing the tests and passing\xa0 the QA review, it's assumed that the new change is good. And if a problem does arise, a new gate\xa0 can be added to the automation so that in the future problems don't occur. The second pillar is\xa0 deployment automation. And the famous post from 2000s, the founder of Stack Overflow, places Can\xa0 you make a build in one step as the second most important question for a development organization,\xa0 and things haven't really changed since then."}),"\n",(0,a.jsx)(t.p,{children:"The efficiency of the build process isn't the\xa0 only goal of deployment automation. However, other goals include the deployment strategies, I\xa0 talked about Canary deployments where you want to show a feature to one user at a time starting\xa0 new versions of your application without causing downtime. If you have to shut off your\xa0 website, before upgrading it, and then turning on the new version, the visitors that\xa0 visit the website in the middle of the upgrade will notice downtime. So there's clever\xa0 deployment strategies you can do to avoid that."}),"\n",(0,a.jsx)(t.p,{children:"And finally, rolling back versions\xa0 in case something goes wrong. It's easy to overcomplicate the planets. Many\xa0 companies have complex internal platforms for building and distributing releases.\xa0 Broadly, success and deployment automation is finding the appropriate deployment tools to\xa0 fulfill business goals and configuring them. And then ideal world there should be little\xa0 to no custom code for deploying. So off the shelf solutions like Spinnaker and harness are\xa0 wonderful places to start for this sort of thing."}),"\n",(0,a.jsx)(t.p,{children:"Finally, application performance management, even\xa0 the best code can be hamstrung by operational errors. There's a famous case where a user put a\xa0 bunch of spaces at the end of their post in Stack Overflow. And they brought down Stack Overflow,\xa0 which is a very popular developer website, because Stack Overflow hadn't deployed their code\xa0 in a way that would deal well with a bunch of whitespace. So a bunch of space characters at the\xa0 end of a post, even with the best code, and even with the simplest things like just a messaging\xa0 board, it's easy to have faults that make it to"}),"\n",(0,a.jsx)(t.p,{children:"production and are only uncovered by users. So\xa0 application performance management ensures that metrics like how long it's taking for requests\xa0 to be processed, how many servers are being used, all of those key health metrics are being\xa0 processed. And if there's a problem, like if all of the requests to the landing page are suddenly\xa0 taking a long time, the appropriate people can be notified automatically, instead of an engineer\xa0 discovering on Twitter that their website is down. Logging. So as a program executes, it will\xa0 produce logs. And the logs generally have"}),"\n",(0,a.jsx)(t.p,{children:"information about the state of things.\xa0 It's useful to be able to map back logs, like, you know, a user visited the website with\xa0 information about that user. So what was their IP address? What was their username, what resource\xa0 did they access and what resources were used for fulfilling that access. So if they had to\xa0 load something from a database, and the database was slow, it's useful to be able to say, the user\xa0 had a slow experience because their request was fulfilled slowly. But the request was fulfilled\xa0 slowly because it was fulfilled from the database"}),"\n",(0,a.jsx)(t.p,{children:"slowly. So mapping these requests all the way down\xa0 to their constituent components is very useful. monitoring. So again, I mentioned metrics\xa0 and automatically alerting people, but taking the logs and metrics, how slower things how\xa0 much memory is left, and deciding what to do. So if there's a bunch of load, you might decide\xa0 based on the metrics to automatically scale the number of servers, so add more web servers\xa0 as they're being used. Based on the logs, if there's errors, you might want to automatically\xa0 file tickets for engineers to look into them."}),"\n",(0,a.jsx)(t.p,{children:"And if there's a downtime, you might want to call\xa0 someone the person on call so that they wake up and take care of the downtime. And they can drop\xa0 everything, they can have a pager, so to speak. And that's alerting. So alerting is when fault\xa0 is detected, some trigger has occurred based on the metrics, some number of requests or to slow\xa0 things are unhealthy, you know, users are going to notice degraded performance, someone should be\xa0 notified or something should be done some action, a new product, shouldn't dive into DevOps,\xa0 engineering all at once. So all of that I've"}),"\n",(0,a.jsx)(t.p,{children:"talked about our end goals for really large\xa0 organizations like Netflix and Facebook, developers that add automation\xa0 as a situation required. So a new startup with no users building a website. pillars two and three are essentially useless\xa0 outages won't be noticed by anyone. Something like a downtime won't be noticed by anyone. It doesn't\xa0 necessarily matter. You don't even necessarily need to run automated tests, a useful stack for\xa0 someone there would be something like that low"}),"\n",(0,a.jsx)(t.p,{children:"five, or sell or our product where you can get\xa0 staging environments to collaborate with other developers. But that's about as far as you care\xa0 for testing wise, you just get to an environments for every proposed change. And you can play around\xa0 with it yourself to see in a manual QA setting, whether it's good or not a team building an app\xa0 for 10 enterprise users. So enterprise users are much more sensitive to downtime. So test coverage\xa0 and business hours alerting should be priorities."}),"\n",(0,a.jsx)(t.p,{children:"On logging and log aggregation error\xa0 collection, there is popular tools like century and code Cove for automated test running, there's\xa0 tools like bit rise and circle ci vaporizes are known for mobile testing, and for alerting\xa0 There's a famous tool called Pedro duty that keeps track of who should be notified if there's\xa0 a downtime. And so during business hours, you might assign someone to be the person\xa0 that isn't supposed to take any meetings for the day. If there's a downtime, they\xa0 will drop everything and solve the problem."}),"\n",(0,a.jsx)(t.p,{children:"And a social media app like Reddit might be using a large combination of things so\xa0 century for catching errors in the website, Elasticsearch LogStash, Kibana is a popular\xa0 way of collecting and looking at logs. pingdom will check whether certain\xa0 pages are taking too long to respond. launchdarkly lets you add feature flags. So\xa0 you can say whether a feature is enabled for some group of users or not, should the new landing\xa0 page be shown to users in North America or Europe, in terraform, lets you automate the deployment\xa0 process. So given a set of servers and a set of"}),"\n",(0,a.jsx)(t.p,{children:"things that need to run on the servers, terraform\xa0 will help you automatically create a plan to ensure that the right things\xa0 are running in the right places. And the conclusion of all of this is that DevOps\xa0 engineering is vital for developer teams. By being cognizant of its three pillars, customers will\xa0 have a confusing and disappointing experience, you know, things will go down, things\xa0 won't scale properly, things will be slow. And so it's really important to keep the\xa0 three pillars in mind as you're scaling an"}),"\n",(0,a.jsx)(t.p,{children:"engineering organization, or if you're being\xa0 hired as a DevOps engineer. new products don't need to automate very much. However, as the\xa0 product matures, and it gets more users, it's more and more important to automate DevOps\xa0 engineering and to dedicate more resources to it. Very good morning to code review automation. Let's\xa0 talk about testing, which is going to be really vital baseline information for when we talk about\xa0 continuous integration, and other code review automation topic. So test driven development is\xa0 a coding methodology, where tests are written"}),"\n",(0,a.jsx)(t.p,{children:"before the code is written. And, you know, we're\xa0 gonna explain tests and test driven development in terms of coffeemakers. So enjoy this picture\xa0 of a nice coffee maker, as we continue. test driven development spun around for a long\xa0 time. It was popularized in the early 2000s. And the idea is simple, but it requires knowledge of\xa0 how things came to be for it to really make sense. So, historically, common words in software\xa0 development, like quality assurance, QA, and unit test have roots in factories building\xa0 physical products. If you were running a factory"}),"\n",(0,a.jsx)(t.p,{children:"building coffeemakers, you would test that\xa0 it worked at varying levels of completion. So unit tests, ensure individual\xa0 components work on their own. Does the heater work? Does the tank hold\xa0 water? integration tests? ensure a few components work together? Does the\xa0 heater heat the water in the tank? system end to end tests? ensure\xa0 everything works together? Does the coffee maker brew a cup of coffee acceptance tests after being launched, sent to\xa0 customers? Are they satisfied with the result? Are"}),"\n",(0,a.jsx)(t.p,{children:"they confused with the button layout or breaking\xa0 the coffeemaker within their warranty period? All of these tests have software analogies, it's\xa0 useful to know which components break in order to diagnose a problem. But it's also useful to\xa0 know that the whole system is working correctly. Because even if every individual component works\xa0 on its own, and if your coffeemaker doesn't heat water with its heater, that's going to be\xa0 a problem when it comes to making coffee. That's really the idea for testing. But\xa0 let's get into test driven development,"}),"\n",(0,a.jsx)(t.p,{children:"which is the methodology built on top of\xa0 testing that's become so popular in the past 10 or 20 years. Most developers that aren't using\xa0 test driven development have a similar workflow, they'll choose something to work on. Based on our\xa0 idea of DevOps, it would be in the planning phase, the developers would find something\xa0 to work on in the planning phase, they build it, so they'd write code, and they'd\xa0 make a build from that code. And then they test it. So they've read small scripts that made\xa0 sure that their code was working correctly."}),"\n",(0,a.jsx)(t.p,{children:"If you're making a function that adds two numbers,\xa0 you might pass it to into unexpected The result is four. And that would be a good indication\xa0 that your function was working correctly. So steps one and three, as it turns out, are\xa0 very connected. The tests written at the end essentially codify the specification, what is\xa0 success for building a coffeemaker, it should heat up in five seconds. So write a test for that.\xa0 It shouldn't brew coffee have sufficient strength, so write a test for that, and so on. test driven\xa0 development uses the similarity of steps one in"}),"\n",(0,a.jsx)(t.p,{children:"three to flip this process. So first, developers\xa0 choose something to work on. And then they write the tests before reading the code. So they\xa0 write tests that are currently failing because the specification isn't satisfied. And then they\xa0 write code, until all of the specifications they wrote in step two are satisfied. So they might\xa0 make a testing regimen that would work if the coffeemaker succeeded, and then build the cheapest\xa0 coffeemaker, which satisfies that testing regimen. And the end result is the same. So the software\xa0 is built, it's tested, and it matches the"}),"\n",(0,a.jsx)(t.p,{children:"specifications. But it's significantly\xa0 easier in a lot of cases to write code. If you write the tests first because you know what\xa0 you're building and it forces you to think about which things are important to work on and which\xa0 things can be put into a later set of change. So this is a very quick video to discuss\xa0 testing. And the next video, we'll talk about continuous integration, which is really the\xa0 DevOps continuation of this idea. See you there. So we've talked about testing, where developers\xa0 read scripts that make sure that their code"}),"\n",(0,a.jsx)(t.p,{children:"continues working way off into the future years\xa0 after they've made their code. And that leads us into our discussion of ci, which is really\xa0 one of the big topics that people talk about in a DevOps context. And ci stands for continuous\xa0 integration. It refers to developers continuously pushing small changes to a central repository\xa0 numerous times per day. And those changes are verified by automated computer software that\xa0 runs the tests that the programmers have defined. So we've gone over what tests are. So let's\xa0 talk about why a company would use ci."}),"\n",(0,a.jsx)(t.p,{children:"Well, ci is really the first step in automating\xa0 DevOps. Imagine the very simplest scenario where a single developer is making a program\xa0 that'll be used by a small group of users. That developer makes the original program releases\xa0 it, and the project slowly builds traction. Now, imagine that developer has\xa0 a critical bug a year later. And they go back to the old code, and they\xa0 say, like, gee, this is really bad code. I've become a better programmer since a year\xa0 ago. I don't really understand what's going on"}),"\n",(0,a.jsx)(t.p,{children:"here. But that's really how development works.\xa0 Programmers get better year after year. And they have to read and understand the bad code\xa0 that they wrote just a year ago. And the only way to be confident making changes to that legacy\xa0 code that might just be a year old, is to have ci ci improves developer speed, because new changes\xa0 can be made confidently without having to worry about breaking existing functionality. As long\xa0 as the tests pass ci also reduces customer churn. problems in the software are much less likely to\xa0 occur. If you have comprehensive tests that run"}),"\n",(0,a.jsx)(t.p,{children:"automatically. As long as you get those check\xa0 marks, you can be reasonably sure that the core features of your application will continue\xa0 working. So how would you integrate ci into your development process? First, let's talk about the\xa0 common branch based development process that many development teams use. So first, developers\xa0 work on a feature branch. So they'll take the files that are most current, the ones\xa0 shown to customers at a specific set of time, they'll branch off of it. So they'll make\xa0 a new copy of the files to work on their"}),"\n",(0,a.jsx)(t.p,{children:"their feature independently of all of the other\xa0 developers working on things that make changes to the various components. So this feature makes\xa0 a change to the mobile app, and to the website. And then on that branch, they'll push it back\xa0 to the repository, which is usually something like GitHub, git lab or Bitbucket. And then\xa0 that repository will run ci, so the CI will be configured on the repository side, it'll run\xa0 all of the tests that the programmer has defined. And then the results of those\xa0 tests will be attached to the"}),"\n",(0,a.jsx)(t.p,{children:"pull request. And the pull request is the\xa0 developer asking to take their code and to merge it into the central repository that users will\xa0 be shown. So you take the feature branch here, and you put it at the end, and all of the\xa0 other commits that are being shown to users. And so this commit is now the one\xa0 that will be shown to users next, and the next time is a deployment, the features\xa0 that the programmer meet will be visible to users. And the best part is it doesn't cost you\xa0 anything a central Git repositories like GitHub,"}),"\n",(0,a.jsx)(t.p,{children:"git lab and Bitbucket. Most have generous\xa0 free tiers, even for organizations minus some security and access control, you know, permissions\xa0 features that you might need. As you scale up, and ci providers like layer ci, GitHub actions, git\xa0 lab pipelines, all have generous features as well. Their ci, you know, is really made for people\xa0 working on websites, that's maybe something to consider. But if you're really early on in your\xa0 projects lifecycle, it doesn't really matter which ci provider to use. Of course, there's one\xa0 thing to take away from the discussion of ci,"}),"\n",(0,a.jsx)(t.p,{children:"it's that ci is a vital tool, it's really\xa0 the first thing that should be automated in most pull request automation schemes. Because\xa0 it's so easy. developers should be writing these tests regardless. And so if you don't run the\xa0 tests automatically, slowly, people will break things without realizing that they're breaking\xa0 them. And users will notice those broken things. And following best practices like feature\xa0 branches and ci is a really easy way to scale a developer team. With just ci,\xa0 a developer team can easily scale from"}),"\n",(0,a.jsx)(t.p,{children:"one to 10 developers. And at some point in\xa0 there, you'll have to start worrying about other pull request automation topics, like\xa0 the ones we'll cover in the next section. talk a lot about theory. But let's\xa0 get practical for a little bit just to round out our understanding of\xa0 how these DevOps concepts work. Let's look at what setting up ci\xa0 looks like for an actual repository. torey This is the live chat example. It's\xa0 an open source version of slack. That's used as a demo repository throughout layer\xa0 three is internal documentation. Let's say for"}),"\n",(0,a.jsx)(t.p,{children:"this open source version of slack, we'd like to\xa0 run tests every time a developer proposed changes, so that in the pull requests tab, we'd be\xa0 able to know whether a change was good. In particular, let's say a developer\xa0 was changing the color of the website. In the main website, After you log in the top bar in sidebar purple, perhaps the customer has requested\xa0 that the color be blue. Instead, if we asked a developer on our team to make this\xa0 change, they would go to the necessary design file"}),"\n",(0,a.jsx)(t.p,{children:"and edit the color. In this case, there are two colors to change. If the developer opened this pull request, it'd\xa0 be very difficult for us to review their change. Without a CI system, all we\xa0 can see is the file change and the description of the commit. So we can see\xa0 that they've edited main dot CSS, and that they've changed these color values. But it's very hard\xa0 to understand the ramifications of this. And it's especially hard to understand whether this will\xa0 have negative side effects for existing users,"}),"\n",(0,a.jsx)(t.p,{children:"especially for changes that are less\xa0 trivial than just changing a color. For this request, if I was asked to review it,\xa0 I would have to pull these changes onto my local developer machine, run the script locally, and\xa0 then evaluate the changes locally. Or I could ask the developer to set up a screen sharing session,\xa0 and they could walk me through the changes. Both of these add a lot of friction to the development\xa0 process. It'd be better if I could evaluate their changes without needing any involvement\xa0 at all entirely through a web interface."}),"\n",(0,a.jsx)(t.p,{children:"But continuous integration helps. Continuous\xa0 Integration allows developers to set up comprehensive tests, so that if something\xa0 doesn't work anymore, after a proposed change, it says right in the pull request. Let's close this change for now, and look at\xa0 the repository to understand how to set up ci. And this repository. One of the services is called\xa0 Cypress. And it's an end to end Testing Service. It contains several configurations.\xa0 And these configurations interact with the page with a fake browser."}),"\n",(0,a.jsx)(t.p,{children:"For example, this test enters\xa0 a username and password, and then logs in and then ensures\xa0 that the user is actually locked in this test goes to the message area enters a\xa0 random message and ensures that the message has actually been submitted that it's\xa0 viewable in the remaining chat area. With enough end to end tests, you can be reasonably confident\xa0 that a chat system like this one continues working. So we'd like to run these tests every\xa0 time a developer proposes a change. To do so we'll have to\xa0 install a plug in into GitHub,"}),"\n",(0,a.jsx)(t.p,{children:"set up the server to run after every pull\xa0 request and run this test against the new server. To do that, let's set up layers here. For our use cases, it's easy to just\xa0 install it directly onto our GitHub account. We can now install it onto our GitHub repository. And now, it's listed here. This means that we've successfully installed\xa0 Lera ci onto this repository. However, nothing will happen yet.\xa0 Because there are no configuration files, we need to set up a configuration\xa0 file for this repository that will"}),"\n",(0,a.jsx)(t.p,{children:"start the whole stack and then run\xa0 the tests in Cypress as required. Let's do that now. Because our\xa0 repository is Docker compose based. Let's use the Docker compose\xa0 example as a starting point. Here, we're going to install Docker, which\xa0 is a containerization technology. We'll talk more about containers versus virtual\xa0 machines later on these sets of talks. Install Docker compose, which is again a way of\xa0 running multiple containers at the same time, these these concepts will become\xa0 clear later on in this talk."}),"\n",(0,a.jsx)(t.p,{children:"We copy the repository files into the test runner. We build all of the services, we start all of\xa0 the services and then we deploy the pipeline. Let's skip the blank for now we'll talk about that\xa0 in the deployment section of this DevOps course. And after all the services\xa0 are started, let's run tests. Luckily, I've already pre set up a script\xa0 for this, so I can copy my configuration. So to recap, what this configuration will do is install the necessary software, in\xa0 this case, Docker and Docker compose,"}),"\n",(0,a.jsx)(t.p,{children:"copy the repository files,\xa0 build all of the micro services, start them all locally within the test\xa0 runner, and then run our tests against them. So now that we've installed\xa0 layer ci onto our repository, all we have to do is add this configuration,\xa0 and we'll have set up ci for it. So let's click Add File, we'll name it layer file. This is\xa0 how layer C is configuration files and other ci providers will have\xa0 different file names, of course, we'll copy our configuration. And we'll commit the file."}),"\n",(0,a.jsx)(t.p,{children:"So now that we've set up ci, we can see that\xa0 there was a dot next to the commit name. And that dot turns into a checkmark\xa0 when the tests have passed. This means that every time a developer pushes\xa0 new code and our source code management tool, look at a success metric. Namely, whether\xa0 the tests have passed or not automatically, they won't have to run the test themselves.\xa0 And the reviewer won't have to trust that the original developer has actually\xa0 tested that the change works. So let's go back to our original\xa0 proposed change of changing the colors"}),"\n",(0,a.jsx)(t.p,{children:"in production from blue to purple. Here, we're going to make our\xa0 change and reopen the pull request. But because we've configured a CI provider\xa0 for it, we'll be able to see that the tests are running automatically directly\xa0 in the pull request view itself. Now, when our developer asks us for a review,\xa0 it'll be much easier for us to be able to tell whether the change has negatively affected our\xa0 customers workflows. In particular, because we've configured Cypress and later ci to check that\xa0 logging in and posting messages still work. Well"}),"\n",(0,a.jsx)(t.p,{children:"know that for this change, even though many files\xa0 might have been changed, the core workflows still work, which gives us a degree of confidence that\xa0 nothing terribly bad has happened with the code. So we can look at the file change for our\xa0 first idea of what the developer has done. And then we can view what the CI is doing.\xa0 So if we open the relevant pipeline, we'll see that the tests are in progress of\xa0 running, the new version of the application has been built and started within the CI runner.\xa0 And the tests are running one by one. Here,"}),"\n",(0,a.jsx)(t.p,{children:"it's tested that you can post chat messages\xa0 within our alternative slacks chat page, the landing page loads, and\xa0 logging in works correctly. So now within our pull request view, we'll be\xa0 able to see a big checkmark here, which shows that all of the relevant ci checks have passed.\xa0 And then you can even automate within GitHub or other source code management platforms that\xa0 certain checks must pass entirely. So you can automate that all ci checks must pass before\xa0 a change could be merged. Let's make sure that"}),"\n",(0,a.jsx)(t.p,{children:"developers are never reviewing code that's so\xa0 obviously broken that it's breaking your tests. And you don't only have to run end to end tests\xa0 here. You can also run linters unit tests and other versions of tests, which we talked about\xa0 throughout this series of talks. And now that I'm happy with the change, I've reviewed\xa0 the files, and I see that the CI has passed, I can merge it with a great deal more confidence\xa0 than if I didn't have this automation in place. That's it for setting up ci and an applied"}),"\n",(0,a.jsx)(t.p,{children:"setting. Let's get back to\xa0 theory for a little bit. Continuing on the topic of testing\xa0 and continuous integration, let's talk about code coverage. So code coverage\xa0 quantitatively measures how comprehensive The tests for a code base are, you might think\xa0 that you have enough tests to find all of the common bugs and to really check all of the\xa0 functionality of your app. But it's hard to put a number on it. Unless you're measuring\xa0 code coverage. This is what a code coverage graph looks like from a popular tool.\xa0 Each of these squares represents a file,"}),"\n",(0,a.jsx)(t.p,{children:"and the color represents how many tests are\xa0 covering that file. So bright green means 100% of the file is tested. And bright red means\xa0 none of the file is tested. So that would be a priority of a file that should either\xa0 be tested or excluded from the measurement. So let's say you're taking\xa0 over an existing code base, it's relatively large at 100,000 lines of code. Over the years, it's been adopted by a couple\xa0 100 users, and you're expected to maintain it and add features without harming those users. So\xa0 the first place you look at is the unit tests,"}),"\n",(0,a.jsx)(t.p,{children:"which we discussed earlier. But they weren't\xa0 really prioritized by the previous maintainers. So there's a mismatch of libraries and naming\xa0 conventions. And it's kind of hard to tell which tests are testing which files\xa0 and which files need to be tested. And before you write any new features,\xa0 you'd like an objective way to measure how sensitive certain parts of the codebase are to\xa0 being changed. If something has very comprehensive tests, you'll be much less scared to make changes\xa0 and add features that touch that part of the code"}),"\n",(0,a.jsx)(t.p,{children:"than if there's a part of the code that doesn't\xa0 have pests. So this is where code coverage really shines. You've got a complicated code base that\xa0 has existing users, you'd like to enforce that tests are written so that things aren't broken\xa0 in an objective way. So getting into the first code of this whole series, let's look at this\xa0 JavaScript function, which I will make bigger. So this is a very simple\xa0 function, if not a bit contrived. It takes a number. And it defines a few variables. It loops up to\xa0 that number, pushing strings into a results list."}),"\n",(0,a.jsx)(t.p,{children:"And then every 50 elements, it pushes\xa0 a special string into the results list. So this whole function is 10 lines of\xa0 code. But not all 10 lines are equal. So really, there's three kinds of lines in a\xa0 program like this. There's the syntax lines, like these closing ones that don't actually\xa0 have any code in them. They're simply syntactic constructs for the programmers benefit.\xa0 They don't you know, it doesn't even make sense to test these because how would\xa0 you test that a semi colon existed or not?"}),"\n",(0,a.jsx)(t.p,{children:"There's logic lines like this one, which\xa0 actually have side effects. And by side effects, I mean that these lines, if you remove them,\xa0 would change the behavior of the program. And those branch lines like this one,\xa0 which changed the flow of the program. So for loops, and if statements in programming are\xa0 used, as constructs that change the order of the commands that run, so this if\xa0 statement, if it evaluates to true, would run this line. And if it didn't evaluate to\xa0 true, it wouldn't run this line. So to reiterate,"}),"\n",(0,a.jsx)(t.p,{children:"the three kinds of lines are syntactical ones that\xa0 don't do anything. The actual logic ones that have effects, and the branch ones that\xa0 change which lines of code execute. And code coverage is usually defined\xa0 as line coverage. So it's the ratio of the non syntax lines which are executed by\xa0 tests over the total number of non syntax lines. So again, consider this test. If you expect the function\xa0 should work with the input to and you manually calculate what the\xa0 function should return for the input to"}),"\n",(0,a.jsx)(t.p,{children:"this would be a unit test for your function. But since you're only executing it on the input\xa0 to this if statement, which requires an input of at least 50 to execute, wouldn't run. So you'd\xa0 be testing this line, it would execute this line, it would execute and this line, which\xa0 would also execute, so you'd be executing five out of six lines, and the deed that would be 83% test\xa0 coverage. So just the single test gets us most of the way to understanding our\xa0 function and understanding its problems."}),"\n",(0,a.jsx)(t.p,{children:"related concept is called branch coverage. So\xa0 instead of measuring how many lines of code it measures groups of lines, in our example,\xa0 above, there's only two branches. There is the main branch. There's the body of the for\xa0 loop, and there's actually a third branch called The if statement body. So here at this line\xa0 will always execute the body of the for loop. will only execute if i is less than n. so here\xa0 you need and to be greater than or equal to one, or these lines to execute. And this line will\xa0 only execute if i is greater than or equal to 49."}),"\n",(0,a.jsx)(t.p,{children:"And so branch coverage would be how many\xa0 individual branches out of these three are evaluated to true by a test. So you'd like\xa0 to know how many of all of the branches are tested. And this is useful because if this\xa0 line of code executes, then this line of code will always execute. So treating them both as\xa0 individual things that need to be tested, doesn't really mean as much as taking the bodies of these\xa0 statements as things that need to be tested. And if you measured the test with branch coverage,"}),"\n",(0,a.jsx)(t.p,{children:"you'd see the two of the three branches\xa0 or x evaluated during the test. So when should you care about this line coverage\xa0 in branch coverage, we've already discussed one scenario where you've inherited an existing code\xa0 base. However, it's important in many different situations. In general, you should measure\xa0 and optimize for code coverage. If any of the following are true. Your product has users.\xa0 And those users might leave if they're affected by bugs, in which case, it's important to\xa0 measure code coverage because it lets you"}),"\n",(0,a.jsx)(t.p,{children:"work with your team to improve the code coverage\xa0 and reduce the number of bugs. You're working with developers that aren't immediately\xa0 trustworthy, like contractors or insurance, that you're bringing them into your code base,\xa0 they need to make changes in some fixed timescale, like form up internship. So they can immediately\xa0 become experts in the entire code base. And you'd like them to be able to make changes\xa0 without worrying too much about things breaking. Or, if you're working on a very large code\xa0 bases, many individually testable components."}),"\n",(0,a.jsx)(t.p,{children:"Your code coverage analysis can complement test\xa0 driven development, which we talked about in the previous talk, to make sure that everyone on the\xa0 team is generally working on important things, and that the things they make\xa0 won't break in the future. So it's a common mistake in code review automation\xa0 to make things too rigid before the product has enough users, if you force developers to get\xa0 100% branch coverage, so to write two to five unit tests for every function, it's going to\xa0 make them much slower at developing features"}),"\n",(0,a.jsx)(t.p,{children:"that users will actually notice. Remember\xa0 that tests are never viewed by users. So the only thing that users care about is the\xa0 stability of the system. So if you have an MVP, or if you have a product that doesn't have very\xa0 many active users, yet, it might not be worth it to measure or optimized for branch coverage.\xa0 until those users care a lot about stability. And by writing unit tests, and other types of\xa0 tests, an important thing to keep in mind is that developers are solidifying the implementations\xa0 of features that they might have to throw out."}),"\n",(0,a.jsx)(t.p,{children:"If you build a feature, and it ends up not\xa0 being something that your users actually want, it's always a better idea to throw out\xa0 that feature, then build it long term. So if a developer builds a feature, and writes many\xa0 tests for it to improve the code coverage of that feature, they'll be much more likely not to throw\xa0 it out, because they'll feel a sense of ownership. And they'll feel a sense of sunk cost in having\xa0 built this feature and made it good, so to speak. So it's important not to over optimize for\xa0 these things before, they're important. And"}),"\n",(0,a.jsx)(t.p,{children:"it's a subjective idea. But really, you'll notice\xa0 when your users start complaining about stability. So organizationally, there are some\xa0 common policies related to code coverage. The first one is useful when you inherit a code\xa0 base. And the policy is that code coverage must not decrease. This one is one of the easiest ones\xa0 to automate. It's especially useful if you're taking over an existing code base, as I mentioned.\xa0 And the idea is that code coverage ratio should never decrease. If the current code has 75% of\xa0 its lines tested, and your new change introduces"}),"\n",(0,a.jsx)(t.p,{children:"40 lines of code, at least 30 of those lines\xa0 will need to be tested. Otherwise, your changes, code coverage would be less than 75% 30 out of 40.\xa0 And you'd be decreasing the average code coverage. As with most code coverage policies, this will\xa0 increase stability, so there'll be less bugs because things will be better tested at the\xa0 expense of developer speed. So developers will have to make some complicated tests,\xa0 and they might spend a lot of time making testing infrastructure. So features will be\xa0 shipped less quickly if you make this sort of"}),"\n",(0,a.jsx)(t.p,{children:"policy. And if you enforce it\xa0 with code review automation. unfortunate side effect of this policy is\xa0 that changes that would be harder to test such as integrations will be less\xa0 likely to be worked on by developers. So developers are incentivized by\xa0 their paycheck and by their manager to ship features quickly to make many features\xa0 per Scrum cycle. And so if certain features are harder to test, because they require internet\xa0 connectivity, or connected third party API's, those features will be harder to make and\xa0 harder to test. And so developers will be"}),"\n",(0,a.jsx)(t.p,{children:"less likely to make them regardless of\xa0 whether they're important to the users or not. So it might be useful to have an exemptions policy\xa0 in place for things like third party integrations. If your organization decides to go for this\xa0 code coverage must not decrease policy. Another useful policy is code owners for test\xa0 files. If you've used code coverage automation to keep code well tested, it's often beneficial to\xa0 define code owners for the tests themselves. This means that developers can change implementation\xa0 details without formal reviewers. But logic"}),"\n",(0,a.jsx)(t.p,{children:"changes. So the tests define what success means\xa0 for a function or for an algorithm, then, changing the tests for a new implementation would need\xa0 to be approved by a senior developer or manager. in GitHub, with an engineering manager, a GitHub\xa0 code owners file might contain this, which means that spec dot j s is a common JavaScript testing,\xa0 naming convention. And at engineering manager username means if, if there's a file called code\xa0 owners, which contains this, then the engineering manager will need to approve any change, which\xa0 changes a test, which is probably a good policy."}),"\n",(0,a.jsx)(t.p,{children:"So if you're working in a large code base\xa0 with test driven development, especially, or if you're hiring interns, or contractors, or if\xa0 your users are especially sensitive to bugs, and you're afraid that they'll have a bad experience,\xa0 if the even small bugs make it to them, it might be in your team's best interest to\xa0 install a code coverage measurement tool. And at the time of writing, these\xa0 are the three most common ones in the open source world code coverage,\xa0 coveralls and Code Climate."}),"\n",(0,a.jsx)(t.p,{children:"So we've talked about testing, and we've talked\xa0 about continuous integration. And those are really like the initial things that are set up\xa0 in a DevOps code review automation pipeline. But the problem is that it requires the\xa0 developers to be on board. And of course, developers are probably busy building\xa0 features and might not necessarily want to make tests or improve test coverage.\xa0 So let's talk about linting, which is something that approximates testing, but\xa0 doesn't need the developers to spend any time."}),"\n",(0,a.jsx)(t.p,{children:"linters are programs that look at a program source\xa0 code, and find problems automatically. They're a common feature of pull request automation, because\xa0 they ensure that obvious bugs do not make it to production, obvious here in quotes. So an example\xa0 of linting. Let's again, look at a JavaScript program, the very simple one, you should\xa0 understand even if you don't know JavaScript, it defines a variable var x equals five,\xa0 and defines a function but continues after the open bracket, which is generally\xa0 considered bad practice. It uses let for the"}),"\n",(0,a.jsx)(t.p,{children:"second variable and defines it with the same name\xa0 as the first one. So this is just confusing, and, you know, wouldn't be called good code, a code\xa0 reviewer would mentioned this in a code review. And then it says while x is less than 100,\xa0 console, log x, and then it closes the while loop on this line, and it messes up the end Det. These\xa0 three lines should be indented for consistency, and then it closes the function. Finally, you\xa0 should realize that this while loop goes forever. x isn't incremented in the body of the while\xa0 loop. So just by looking at the code statically"}),"\n",(0,a.jsx)(t.p,{children:"without running any environment or looking\xa0 at the code with a browser even, you can tell that this loop will run forever, and that's\xa0 probably something a programmer didn't intend. So much of this feedback could be automated, a set\xa0 of rules like don't shadow variables. Never name a variable in an inner scope that has the\xa0 same name as a variable in an outer scope could be applied to each proposed change, so that\xa0 human reviewers would not have to waste effort leaving code style comments. tools that\xa0 maintain and run such lists are called linters."}),"\n",(0,a.jsx)(t.p,{children:"relevantly, another class of code review\xa0 feedback has to do with code style. It's easy for coder bureaus to waste time pointing\xa0 out stylistic choices like tabs versus spaces, or camel case versus pothole case. These\xa0 discussions bring no value to end users, you know, your customers don't care what case\xa0 your code is written in. And ultimately, they just serve to cause resentment and missed\xa0 deadlines within engineering teams. If a review takes an extra couple hours\xa0 because of comments like this, that's a"}),"\n",(0,a.jsx)(t.p,{children:"couple hours that the programmer could have been\xa0 focusing their attention on another feature. So engineering organizations should eventually\xa0 adopt to maintain a global style guide. But in most cases, just starting with\xa0 something like the Google Style Guide, which is open source and available at\xa0 this link, is a great starting point. These guides often come with linter\xa0 configurations, which help everything Stay stylistically similar, and some programming\xa0 languages like Python and go come with their own"}),"\n",(0,a.jsx)(t.p,{children:"style guides and automation, like Pep\xa0 eight. In the case of Python that will make it easy for developers using those\xa0 programming languages to stay in a unified style. an organizational thing you can do for code\xa0 style is to knit, or which stands for nitpicking. Instead of blocking at the code review stage, if\xa0 there's review feedback that a code style review feedback, it might be better for code reviewers\xa0 to leave small review comments called mitts. So they'd say knit full colon\xa0 shouldn't be styled this way."}),"\n",(0,a.jsx)(t.p,{children:"This is great. Because it allows the reviewers to merge something with a few pieces of feedback\xa0 so that ours don't have to be spent on a small piece of refactoring that could be\xa0 done in a later state at a later point of time. Once the style guide is adopted, it's possible\xa0 to configure tools to automatically format code to follow the Style Guide, which tools are called\xa0 Auto formatters. And the programming language go, which we use at layer ci, a command such as\xa0 the following would use the standard format"}),"\n",(0,a.jsx)(t.p,{children:"are the one that comes with go to clean up\xa0 all of the source files in the repository. So we'd use the Janu, find command,\xa0 find the files that have a go extension, and we'd exec go format on them. And this\xa0 will take all of the source files and then format them with their style guides\xa0 so that they all pass the style guide. And of course, if your ci system is running tests\xa0 automatically, every time your code is pushed, the code could be automatically limited as\xa0 well. Programmers shouldn't have to wait for"}),"\n",(0,a.jsx)(t.p,{children:"a human reviewer to tell them whether the code is\xa0 limited and styled appropriately. In most cases, it's cheap and convenient to run linting and\xa0 formatting automatically with a CI system. So an easy solution is you get another\xa0 checkmark. To get something set up quickly, it's a good start to make lint act the same as\xa0 running a unit test in ci, so add an x if the code isn't listed properly, and then the developer\xa0 can very quickly get stylistic feedback without needing to talk to another human or wasting their\xa0 reviewers time for getting this sort of feedback."}),"\n",(0,a.jsx)(t.p,{children:"And a CI configuration that might look like this. So copy the project files, run the linting the\xa0 script. And then if the linting script fails, the whole pipeline would fail. This approach\xa0 stops reviewers from the picking style, it passed the linter is a perfectly reasonable response to\xa0 an overly zealous code reviewer. So even simple automation like this can improve the development\xa0 speed of entire development teams. It also stops reviewers from having to give style feedback at\xa0 all, at all of the checks for code review pass,"}),"\n",(0,a.jsx)(t.p,{children:"like it passes all of the linters\xa0 done the committed stylistically Okay, the reviewer might still leave some feedback for\xa0 future reference, but they shouldn't be blocking commits getting to production because of small\xa0 stylistic choices that aren't even in the linter. A better long term solution is\xa0 to set up a commit back button, which is common idea that happens all over\xa0 the place and code review automation. But in this specific example, it might look like\xa0 this. So you'd say if the code is not limited,"}),"\n",(0,a.jsx)(t.p,{children:"run, yes, lint with the dash dash fix flag, which again, goes through all of the source\xa0 files. Yes, lint is the linter for JavaScript. So this would go through all of the source files.\xa0 For each of them, it would apply the linting rules, and it would fix any stylistic\xa0 errors. And then it would create a commit an additional, you know set of file changes\xa0 on top of what the developers proposing. And it would create a new branch with a suffix\xa0 listed. And it would push to that branch. So that developer pushes unlimited code, the bot\xa0 would automatically create a commit which lifted"}),"\n",(0,a.jsx)(t.p,{children:"everything and it would create a new branch so\xa0 that if the developers code was known to be good, the reviewer could simply merge the Linton\xa0 branch instead of the developers original one. And then we failed the pipeline with lint\xa0 failed. This means that the unlimited version can't be merged. But the limited one, assuming\xa0 all the feedback that isn't listed related, could be merged. So we'd have two branches, the\xa0 one the developers proposing and the limited one, the code reviewer would look at the one that\xa0 wasn't rented. They'd say whether it was good"}),"\n",(0,a.jsx)(t.p,{children:"or not, like the logic of the commit was good.\xa0 And if it was good, then the reviewer in GitHub could merge this branch instead of\xa0 the one they were asked to review. This branch would be the same as the original\xa0 one with an additional commit on top of it. So some examples of limiters\xa0 for many programming languages. JavaScript the standard as a 2021 is Eastlands.\xa0 TypeScript. Also now uses Eastlands, Python you piland and flake eight. c++ is much more\xa0 subjective. But a common choice is Google"}),"\n",(0,a.jsx)(t.p,{children:"CPP lens from the Google style guide mentioned\xa0 above, go comes with a format called go format, which acts somewhat like a linter. Although\xa0 there's additional libraries available for rules beyond that, Java has checkstyle and find bugs,\xa0 maybe older options, but there's a lot of choices for languages like Java. And Ruby has broke\xa0 rubocop and pronto. We've seen users commonly use and Java, JavaScript C sharp, and many other\xa0 languages can be lifted with sonar cube, which is a popular static analysis\xa0 framework that is commonly used at"}),"\n",(0,a.jsx)(t.p,{children:"larger enterprises. But it has an open source\xa0 version, that is a good place to start for 10 developer teams that would\xa0 like to set up static analysis. And finally, it was a startup called\xa0 Deep source that we've talked to startup to startup. And they're doing all sorts\xa0 of interesting stuff with static analysis as well. Static Analysis is just the practice of\xa0 looking at source code without running it and finding bugs. So I'd encourage\xa0 you to look at deep source as well. So in comparison to most other code automation\xa0 tools, linters are exceptionally easy to set up."}),"\n",(0,a.jsx)(t.p,{children:"Any team with more than one developer\xa0 should almost immediately set up a linter. To catch obvious bugs like infinite loops. Just\xa0 by looking at the code, the linter would be able to tell you whether there was a common\xa0 programmatic error like an infinite loop. Automatic linting comes standard has many code\xa0 editors. So it would be wise to teach developers how to configure their code editors to use\xa0 the existing linting rules that your team has set up in the CI automation, so that the\xa0 developers don't have to wait to push their"}),"\n",(0,a.jsx)(t.p,{children:"code to get this feedback, they can get the\xa0 yellow squigglies directly in their editor. And then teams working on earlier products.\xa0 Mentors can help avoid writing unit tests at all. Instead of relying on a test\xa0 suite, you can rely often on a static analysis to find common bugs\xa0 like the code not compiling at all or infinite loops or stylistic problems.\xa0 This helps small teams refer their product has many users get feedback without\xa0 needing to lock things in with tests. Right. So that's it for linting and\xa0 code style. We'll see in the next video."}),"\n",(0,a.jsx)(t.p,{children:"Let's finish up our discussion of\xa0 code review automation by talking about ephemeral environments, which are\xa0 really the latest and greatest when it comes to doing code reviews and helping\xa0 developers get their changes merged. ephemeral environments are temporary environments\xa0 that contain a self contained version of the entire application. Generally, for every feature\xa0 branch, they're often spun up by a slack bot, or automatically on every commit using DevOps\xa0 platforms like later ci itself, or Heroku."}),"\n",(0,a.jsx)(t.p,{children:"Temporary environments are overtaking traditional\xa0 ci platforms as the most valuable DevOps code review experience. Because these environments\xa0 are made on every change all of the stakeholders, not just developers, but the product, people in\xa0 the designers can review a change without needing that set up a developer environment or asking to\xa0 screen share with the developer that proposed it. So for a more concrete example, let's\xa0 say developer is changing something on a website. So they're changing, you know,\xa0 the front end or the back end, or, you know,"}),"\n",(0,a.jsx)(t.p,{children:"some component of the website. And they'd\xa0 like to get feedback on the proposed change. So a code reviewer would look at the\xa0 code and they might not understand what the visual ramifications of that change\xa0 are. But within the femoral environments, within the code review view itself, the reviewer\xa0 would just have to click that button there. I'll zoom in. So within GitHub, this is what the\xa0 reviewer would see. They'd See The Description, the code change, but also a button to view the\xa0 ephemeral environment. And when they click that"}),"\n",(0,a.jsx)(t.p,{children:"button, it wakes up a version of the website\xa0 specifically with this proposed change in it so that the reviewer can actually\xa0 take a look at things and see whether the changes is visually\xa0 and workflow wise, working well. In general, ephemeral environments\xa0 like halfway between development environments and staging environments.\xa0 At the extreme staging is entirely replaced by formal environments in\xa0 something called continuous staging. Benefits of ephemeral environments.\xa0 Well, the most common reason to adopt"}),"\n",(0,a.jsx)(t.p,{children:"an ephemeral environment workflow is that is that\xa0 it accelerates the software development lifecycle. Developers can review the results of changes\xa0 visually, instead of needing to exclusively give feedback on the code change itself. Additionally,\xa0 developers can share their work with non technical collaborators such as designers as easily\xa0 assuring a link to the proposed version. So You could post a slack message like this\xa0 saying, could you go to this link and give me feedback instead of needing to\xa0 set up a zoom call to share your screen"}),"\n",(0,a.jsx)(t.p,{children:"to get the other person to\xa0 look at your proposed changes. The hardest part of setting up ephemeral\xa0 environments are dealing with state. So dealing with things like\xa0 databases and microservices, by their nature, you know, ephemeral\xa0 environments are temporary. They're isolated from production environments, and\xa0 really only lasts as long as a pull request does. A reviewer should be able to delete a resource\xa0 in a review. So they should be able to see if you know, deleting a user still works without fear\xa0 of that affecting the production environments."}),"\n",(0,a.jsx)(t.p,{children:"So in the early implementation of ephemeral\xa0 environments, it might make sense to connect API servers with read only permissions to a\xa0 staging database. So if you're using AWS, you might have an Iam role that has read only access\xa0 to the database. But in that case, you wouldn't be able to sign up to the service, for example,\xa0 because that would require database rights. The end goal should be to have a fresh\xa0 copy of the database for every commit. So every time a developer\xa0 proposes a change, they get"}),"\n",(0,a.jsx)(t.p,{children:"the new database specifically for their\xa0 environment that they can do whatever they want in an ideal ephemeral database has\xa0 three attributes. It's pre populated, so it contains representative anonymized data.\xa0 The past security audits of PII, personally identifiable information must be scrubbed\xa0 from databases used in ephemeral environments. It should be undoable. So if in the course of\xa0 review data is deleted, it should be easy to reset the database to its original state. This is also\xa0 crucial for reading destructive end to end tests,"}),"\n",(0,a.jsx)(t.p,{children:"which we'll get into later.\xa0 And it should be migrated, the database should use the schema currently\xa0 used in production, it's not very useful to know if something's working with an old version\xa0 of the schema. One of the most common classes of problems uncovered by formal environments or\xa0 broken or nonperformance database migrations. Another hard problem to solve with ephemeral\xa0 environments is the life cycle. So when would you create them and when you destroy them? The classic\xa0 approach is to title lifecycle of a pull request"}),"\n",(0,a.jsx)(t.p,{children:"to the lifecycle of an ephemeral environments. So\xa0 if the developer opens a pull request, create an environment for them, keep it running 24. Seven\xa0 until the developer deletes the environment. The biggest factor to consider there is cost.\xa0 If each FML environment costs 10% of production. So it's 10% cheaper, and you have 30 open pull\xa0 requests, you'd be quadrupling your monthly costs. So you know, that's an expensive developer tool. Another approach is to create a chat ops bot that\xa0 allows creating new environments for a specific"}),"\n",(0,a.jsx)(t.p,{children:"branch with a specific timeout. So for example,\xa0 the user type slash PR bot creates in the GitHub issue description that could create an environment\xa0 or in Slack, the user could do the same thing. This requires the environment to be provisioned\xa0 at the time that it's required, which can be slow. And it's again, hard to tell when to delete these. The best approach is to create an\xa0 ephemeral environment for every change. So similar to the PR workflow, but\xa0 hibernate them as they're provisioned."}),"\n",(0,a.jsx)(t.p,{children:"There's only a few providers that do this. So one\xa0 is Heroku, which will, with Heroku review app can turn on and off environments. And the other\xa0 one is layer ci, shameless plug, I suppose. So as users use the environments, and layer\xa0 ci, there'll be a hybrid ninja not enough. You can automate this yourself with memory\xa0 snapshotting, but it's somewhat involved. So this might be something that's better\xa0 left to using a third party for. And back to that idea of continuous staging."}),"\n",(0,a.jsx)(t.p,{children:"The idea is to merge staging ephemeral\xa0 environments and a CI pipeline altogether. So this is kind of layer ci itself primarily sells to our\xa0 users. As your ephemeral environments become more powerful and easier to create. They approach and\xa0 overtake many aspects of traditional continuous integration pipelines. So if you can set up\xa0 the website, and the back end and the database, then it's relatively easy to run tests because\xa0 tests are usually much easier to wrap them the entire back end. At its logical conclusion, this\xa0 concept becomes continuous staging, where ci CD"}),"\n",(0,a.jsx)(t.p,{children:"and ephemeral environments form a single\xa0 ci CD flow where a single base sets up all of the requirements for everything. And then that\xa0 forks off into the unit tests but also the server but also how the review environment but also the\xa0 linter is everything comes from that common base. If you're going to make them\xa0 yourself, you should probably budget about a month, or a month of time\xa0 it took to set up your environment. So if your production environment has many\xa0 different microservices and has many different"}),"\n",(0,a.jsx)(t.p,{children:"databases, it'll be relatively difficult\xa0 to set up an ephemeral environment flow. Large companies like Facebook have set\xa0 this up for their internal pull requests. But they haven't hired developer teams,\xa0 infrastructure software engineers that do this. So if you're a smaller company, you might want to\xa0 stick to a hosted service again, like layer ci, instead of making it yourself up to\xa0 maybe when you have about 20 developers. And to avoid having to micromanage\xa0 starting and stopping environments,"}),"\n",(0,a.jsx)(t.p,{children:"it's easiest to use a hosted provider. If\xa0 you're doing just front end development. Some popular choices are for cell netlify. But\xa0 if you're doing full stack deployments, really the only choices available right now are layer\xa0 ci itself and Heroku review apps. There are some options available in many source code platforms\xa0 like Git lab has a environments feature. But it's not really truly an ephemeral\xa0 environments feature. So you should explore all of those options\xa0 make an informed decision."}),"\n",(0,a.jsx)(t.p,{children:"So that was ephemeral environments.\xa0 And that concludes our discussion of code review automation. Because pull request automation is\xa0 such a core part of DevOps engineering, let's do another applied tutorial here. In this example, we'll be setting up\xa0 a femoral environments the same way we talked about before using a hosted\xa0 platform for the sake of simplicity. So because we've already set up ci for this\xa0 repository, we already have our layer file, which is our CI configuration. However, many ci\xa0 providers including layer ci, Roku, and others"}),"\n",(0,a.jsx)(t.p,{children:"can set up ephemeral environments, which\xa0 are small production deployments you can use to evaluate the changes live as a reviewer. Again,\xa0 let's say we're changing the color this time back from blue to purple. And we'd like someone to be\xa0 able to efficiently review our change, not just by looking at the test results, but also by looking\xa0 at the federal environments. To read manual QA, you might see, in this case, it's actually very\xa0 easy to set up. Let's go to our web micro service."}),"\n",(0,a.jsx)(t.p,{children:"Let's create a new file. Here, we'll make another layer file so that they run in parallel. And we'll\xa0 say from our base layer file, we'll say expose website. And we'll expose the website running inside the\xa0 runner itself. Later ci has this expose website directive, but many other providers have\xa0 similar functionality that you can set up. And let's jump right to\xa0 creating a pull request for it. So here, our code reviewer would\xa0 not only see the test results, as you can see, those are\xa0 here, the initial layer file."}),"\n",(0,a.jsx)(t.p,{children:"Let's look at the actual graph to understand better what's going on. Here\xa0 we have our tests running in the main layer file. The main layer file has again built all of\xa0 the services started all of the services. And now it's running our Cypress tests\xa0 the same way it did in the CI chapter. But after the Cypress tests run, we'll have a\xa0 second environment, which is inheriting from the first and that second environment will have\xa0 a clickable link that can be used for manual QA. So let's see that here."}),"\n",(0,a.jsx)(t.p,{children:"The snapshot is done being taken of the tests, which means that the ephemeral\xa0 environment can start being built. And now you can see that it's built a staging\xa0 server button, and you can connect to it here. So in our actual pull request, now that all\xa0 of the tests and ci services have passed, we can click the femoral environment button. As soon as it appears. We could click the main layer file details, we can click the services\xa0 web ephemeral environment, and we can click View website. And\xa0 what this does is it wakes up the"}),"\n",(0,a.jsx)(t.p,{children:"pipeline which we initially set up to run our\xa0 tests. But it forwards the internet visible link to the web server inside. So here\xa0 we've created a fresh environment, specifically for this test, and we can see\xa0 that the test has run and sent the message. And we can evaluate this change so we can\xa0 test that creating channels works for example. And that in the test channel, it's\xa0 still possible to set messages. This means that you don't need 100% test coverage\xa0 to be able to understand the nuance of a channel"}),"\n",(0,a.jsx)(t.p,{children:"For every pull request, you'll be able to\xa0 spin up a new environments automatically, and then wake up that environment\xa0 when a review needs to be completed. And now that we're satisfied with the environment\xa0 works correctly, we can merge the pull request. And from now on, all changes which edit\xa0 the website can be manually curated, so the reviewer can check that things work,\xa0 but also a QA team or a designer or a product manager might be able to check that\xa0 the change actually changes what it's"}),"\n",(0,a.jsx)(t.p,{children:"supposed to do. That's it for formal\xa0 environments. Let's get back to theory. Welcome to DevOps Academy deployments. And this\xa0 one, we'll be talking about foundational concepts. And primarily, when you talk about\xa0 deploying, you're talking about VMs. And you're talking about containers. And\xa0 containers are often also known as Docker. So let's talk about the difference between those\xa0 two. Before we talk about deploying anything. People talk about DevOps deployments, they're\xa0 usually talking about the point that Linux,"}),"\n",(0,a.jsx)(t.p,{children:"a large portion of all\xa0 deployments are to Linux servers. And containers are really only defined in\xa0 terms of Linux, in production as of right now. So with all that in mind, let's\xa0 talk about Linux in the abstract. So what Linux really helps you do is take care\xa0 of four things when you're writing programs, it takes care of memory. So programs need memory\xa0 to do things, memory is also known as Ram. And since you only have a finite amount of it,\xa0 Linux itself needs to figure out which programs"}),"\n",(0,a.jsx)(t.p,{children:"will get which sections of memory, so which ram\xa0 sticks will have which programs running on them. Linux also takes care of processors. So\xa0 if you're running two things in parallel, Linux will make sure that the right amount\xa0 of processors are dedicated to both. If you're, if you've ever run very computationally\xa0 intensive tasks on a laptop, you might notice that your browser gets laggy. That's because\xa0 it's not getting enough processor time. So if you're running production\xa0 workloads, Linux needs to make sure"}),"\n",(0,a.jsx)(t.p,{children:"that every program is getting its fair share\xa0 of processor time to run the actual program. Because disk, so Linux takes the files of all\xa0 programs and allocate space on the disk for them, you might have multiple disks, and you might\xa0 have both spinning disks and solid state drives, you might even have disks shared across networks.\xa0 So Linux takes care of all of that and make sure that the right files are on the right disks,\xa0 and that programs have access to those files. And finally, there's devices. So\xa0 even beyond disk memory, and CPU,"}),"\n",(0,a.jsx)(t.p,{children:"there's things like GPU. So for\xa0 machine learning, you see GPU often, and things like network cards, which\xa0 you use for connecting to the internet. Linux needs to take these individual resources\xa0 and allocate them to processes. So if you have five processes trying to connect to the internet\xa0 at the same time, but only one network card, Linux needs to make sure that the right\xa0 messages are sent to the right websites upstream. And the responses are sent\xa0 to the right programs downstream. So in a diagram, this is\xa0 what that would look like."}),"\n",(0,a.jsx)(t.p,{children:"So here we have three programs, Chrome,\xa0 Notepad, and Spotify. And they're all running in Linux. So this is assuming you have\xa0 a Linux server running these three programs. And here you have the four shared resources. So\xa0 Chrome, asks for CPU, and Linux will allocate some of the CPU time to Chrome. And it'll also\xa0 allocate something notepad and some to Spotify. And similarly for all the other shared resources. So this is great, but there's too much\xa0 sharing going on. What I mean by that is that"}),"\n",(0,a.jsx)(t.p,{children:"programs know about each other. So if you\xa0 had a program that accept expected a file at home calling file dot txt,\xa0 then it could create that file, but another program could delete or read that\xa0 file. so files can be read across programs. And that means those programs can communicate between\xa0 each other, which isn't always what you want. So, for example, programmers often use\xa0 different versions of Python. Python is a popular programming language. And\xa0 there's two popular versions used."}),"\n",(0,a.jsx)(t.p,{children:"One is Python two, and one is Python three, but\xa0 they're both called Python. So if the file at user bin Python is a Python two executable, and\xa0 you try to run a Python three program with it, then that program would error because you'd be\xa0 using the wrong version of Python to run it. However, some of your programs might need Python\xa0 two, and some of them might be Python three. So here, there's cross talk between programs in\xa0 that they're both reading user bin Python, but they expect there to be different files there. So\xa0 that's what I mean by programs overshare sometimes"}),"\n",(0,a.jsx)(t.p,{children:"They need different versions\xa0 of files at the same place. And so you can't really run\xa0 both programs at the same time. Similarly, two web servers might listen\xa0 to Port 80. That's how websites allow you to connect to them. So if you're running\xa0 two web servers that both expects Port 80, to be open, the first one will start\xa0 correctly. And the second one will crash, saying that Port 80 is already used these\xa0 sorts of problems of sharing resources is really where virtual machines and containers\xa0 shine. They allow you to separate resources like"}),"\n",(0,a.jsx)(t.p,{children:"files and ports between programs so that\xa0 programs can't step on each other's feet. So here, if you are running your\xa0 three programs in that container, it would look remarkably similar.\xa0 So Chrome would be running, but it would be running within a container.\xa0 And that container would be talking to Linux, which would then allocate the container\xa0 resources. And similarly for the other programs. Now, this might not make sense yet. But let's talk\xa0 about what actually happens when you put something"}),"\n",(0,a.jsx)(t.p,{children:"in a container like this. So what happens when\xa0 there's a container between a program and Linux? The big change is that each\xa0 program will get its own version of shared resources\xa0 like files and network ports. The container running chrome might create\xa0 a file at tilby slash chrome slash cash. While the container running notepad could\xa0 read that file and see that it didn't exist, they get different copies\xa0 of all of the systems files, so that they couldn't talk amongst each\xa0 other, or have conflicting Python versions."}),"\n",(0,a.jsx)(t.p,{children:"Similarly, if you had two web servers running\xa0 that both expected to be able to open port 81, would be able to open port 80 in their container,\xa0 and the other would be able to open port 80 in their container. So you can have two programs\xa0 both thinking that there are the only program listening on port 80. But really, they'd\xa0 be isolated within their own containers. So in Linux containers work by creating\xa0 namespaces, which are a Linux feature that groups shared resources together. If\xa0 you had five processes running together"}),"\n",(0,a.jsx)(t.p,{children:"within a Docker container, they'd still\xa0 be running within Linux itself. But they would not see the other processes, the\xa0 ones on the main Linux machine itself. So within the container, if you ran PS, au x, and\xa0 you counted how many lines of output there were VSD UX is how you see the running commands\xa0 in the Linux machine, you might see 10. And that means that there was 10 processes visible\xa0 to you within the container. But within Linux, so within the container up here, if you said how\xa0 many processes were running, it would say there's"}),"\n",(0,a.jsx)(t.p,{children:"10 processes. But within Linux itself, you'd see\xa0 hundreds of processes running, including the 10 from this container. So the containers are kind\xa0 of sandboxed or namespaced, into a single group of processes where the processes can't see the files\xa0 outside of the container, or the processes or the network ports outside of the container. They\xa0 only see the ports within this container. So essentially, what's happening is the programs\xa0 are asking, what are the contents of user lib Python, in our example before and instead of\xa0 answering truthfully, Linux is answering with"}),"\n",(0,a.jsx)(t.p,{children:"the contents of another file. So the container\xa0 says what is user lib Python, and then Docker, if you were using Docker for your containers would\xa0 respond with the contents of this file var lib, Docker overlay Fs one user lib Python, which is\xa0 a totally separate file in the global system. So each container would have\xa0 its own view of the files. This little deception allows programs to run in\xa0 parallel, because Linux would be would respond with different files for each container. One\xa0 container could have Python pointing at a Python"}),"\n",(0,a.jsx)(t.p,{children:"two executable, and one container could have\xa0 Python pointing at a Python three executable. So if that's how containers work, then\xa0 how to VMs work, how are they different? Well, VMs are very similar to\xa0 emulators. If you've ever seen someone running an older video game on\xa0 a modern computer, they're using a VM. So the idea for containers\xa0 was to provide fake Linux. Within the container they don't really\xa0 know they're running with within a container. They see files, but the files\xa0 are simply pointing at a different place"}),"\n",(0,a.jsx)(t.p,{children:"within the real Linux installation. The\xa0 idea for VMs is to produce fake versions one level below that. So pretty produce fake\xa0 versions of the CPU, RAM disk and devices. The VM equivalent of Docker\xa0 is called a hypervisor. It's the program which is in charge of creating\xa0 the VMs. So when a VM is running something, it corresponds to an instance\xa0 of the hypervisor within Linux. The hypervisor might lie to the VM and say\xa0 there's one SSD attached. There's one drive attached and then has 50 gigabytes of capacity.\xa0 But then when the VM writes to that drive,"}),"\n",(0,a.jsx)(t.p,{children:"it would instead go to a file, it wouldn't go to\xa0 a real drive. And then on the host, it might be This file. So when the VM itself is writing to\xa0 the file, it's actually going through this file, which is very similar to the deception of\xa0 matching files directly that the container had. But there's some practical differences. The\xa0 first is that VMs are very powerful. You can use them to run other operating systems, such as\xa0 Mac OS, or Windows, and different hardware configurations, you can emulate a gamecube,\xa0 or an apple two within a Linux hypervisor."}),"\n",(0,a.jsx)(t.p,{children:"in containers, it's the processes that are being\xa0 lied to, they must still have been the sort of thing that would run within Linux itself. But\xa0 in VMs, there's a nested operating system that generally doesn't know it's not talking to the\xa0 real hardware. When the OS writes things to its drive, for example, those rights are sent to\xa0 a file in Linux instead of a physical drive. So when the process writes to the\xa0 operating system within the VM, that operating system sends the right to\xa0 a drive, or what it thinks is a drive,"}),"\n",(0,a.jsx)(t.p,{children:"but that drive goes through Linux,\xa0 and Linux actually maps it to a file. Various benchmarks show that CPUs and VMs are\xa0 about 10 to 20%, slower than containers. VMs also usually use 50 to 100% more storage, because they\xa0 need all of the things that an operating system would need. Duplicate containers don't need all of\xa0 the files, they only need the application files. And finally, VMs use about 200 megabytes more\xa0 memory for the operating system itself. Again, containers don't need all of the operating system\xa0 because it's the processes that are being lied to."}),"\n",(0,a.jsx)(t.p,{children:"So VMs use more memory, they're\xa0 slower, and they need more storage. So given these performance benefits, it looks\xa0 like containers are almost always a better choice. And in most cases they are However, there's a few cases where VMs are a better\xa0 choice. Again, VMs mean virtual machines. If you run in untrusted, so user supplied\xa0 code, it's difficult to be confident that they can't escape a container. This\xa0 has gotten better in recent years, but it's long been a contentious point. Virtual\xa0 Machines are much older and much more mature. So"}),"\n",(0,a.jsx)(t.p,{children:"if you're running untrusted code, usually it's\xa0 a good idea to put that within a container. If you're running a Windows or Mac OS script,\xa0 so you're running a script that only runs on another operating system, you'd need to use a\xa0 VM for similar reasons. Or if you're running an old video game that doesn't run in Linux,\xa0 and you'd like to run it on a Linux computer, you'd need to VM. And vice versa, if you're\xa0 using VMs, and other operating systems, if you'd like to run a Linux program in Windows,\xa0 you'd usually have to use a VM to run it."}),"\n",(0,a.jsx)(t.p,{children:"And finally, you can emulate hardware\xa0 devices like graphics cards with a VM. So if you're testing that your graphics card\xa0 works correctly, you could emulate the response that it would give and then test that the\xa0 operating system is working as expected. So that's the big difference\xa0 between VMs and containers. And these are really the two\xa0 things that you often deploy. So let's go into actual deployment strategies\xa0 in the next talk. I'll see you there. Let's keep talking about deployments. So rolling\xa0 deployments are one of the most popular deployment"}),"\n",(0,a.jsx)(t.p,{children:"strategies. And we'll talk about the pros and cons\xa0 of different deployment strategies throughout this section. Rolling deployments themselves work\xa0 by starting a new version of the application, sending traffic to the new version, to make sure\xa0 everything's okay, and then showing off the old version and repeating that until all versions of\xa0 the old version or versions of the new version. I realize I've said version many times. So let's look at pictures that will\xa0 help illustrate the point."}),"\n",(0,a.jsx)(t.p,{children:"This is the myrn app. myrn stands for\xa0 MongoDB, Node JS, react, and express js. Here, the user's web browser connects to\xa0 both the front end and the back end, where front end is the stuff that the user\xa0 sees. And back end is the services that provide connections to the database. So if\xa0 you log in, you're connecting to the back end. If you're just viewing the landing page,\xa0 you're connecting to the front end. Let's say your app has enough traffic that users\xa0 will notice if it goes down for a little while."}),"\n",(0,a.jsx)(t.p,{children:"How would you push a new version of the\xa0 application without causing downtime. This is where rolling deployments come in. The\xa0 high level algorithm for a rolling deployment looks like this. So you create an instance of\xa0 the new version of the backend say, you wait until it's up. So you keep trying to connect to\xa0 it until you get a response that satisfactory. And then you delete an old version and\xa0 route the traffic to the new version. If any instances of the old version still\xa0 exist, go back to step one and repeat."}),"\n",(0,a.jsx)(t.p,{children:"And our myrn example, we'd initially\xa0 see three instances of the initial version and one instance of the new\xa0 version. And we'd repeat the process Until we had three instances of the new version\xa0 and one instance of the initial version. So here, all of the versions are the back end. And we add a new version of the new back end. And\xa0 we turn off a version of the old back end. And we keep repeating that. So as time goes on, the red\xa0 ones replaced the pink ones. And then after a few loops of this, the only ones remaining are red\xa0 ones, we've added a red one, remove the pink one,"}),"\n",(0,a.jsx)(t.p,{children:"added the red one, remove the pink one. And we're\xa0 red is the newest version of the application. So what are the benefits of rolling deployments\xa0 over other ways of deploying things? Well, they're well supported. Rolling deployments are relatively\xa0 straightforward to implement. In most cases, they're natively supported. And several\xa0 orchestrators, if you've heard of Kubernetes, for example, Kubernetes helps you with\xa0 this. AWS, Amazon Elastic Beanstalk also supports rolling deployments."}),"\n",(0,a.jsx)(t.p,{children:"They don't have huge bursts. So in another\xa0 deployment strategy, which we'll talk about, if you had three versions of the back end, you\xa0 need to start six in total, to deploy the new version, and then you turn off the old three.\xa0 So doubles for the duration of the deployments, the amount of things running, which might\xa0 be difficult if you have a finite number of servers, for example. It's also not\xa0 uncommon for services like databases to limit the amount of connections. So if you\xa0 had six versions of the backend connecting to"}),"\n",(0,a.jsx)(t.p,{children:"the database, now that might be too much load\xa0 on the database, so that could cause problems. And really, deployments are easily reverted. If in the course of an upgrade, you notice\xa0 problems, it's usually easy to reverse the rolling deployment by just going in the\xa0 opposite direction, removing a red adding a pink, you can go in the opposite direction as\xa0 well to rollback which is an important characteristic of deploying\xa0 because things always go wrong. The downsides of rolling deployments are they\xa0 can be slow to run. So if you have 100 replicas,"}),"\n",(0,a.jsx)(t.p,{children:"and you're replacing one at a time, and it\xa0 takes 20 seconds, it would take 2000 seconds to replace all of the versions, which\xa0 is quite a long time for deployment. This can be mitigated by increasing the number of\xa0 services being turned on and shut off at a time, which is sometimes called a burst\xa0 limit, or a rolling deployment size. The other problem is API compatibility, which\xa0 is the biggest problem of rolling deployments. So if you add a new version of an API endpoint to\xa0 your back end, and consume it in your front end,"}),"\n",(0,a.jsx)(t.p,{children:"then since you're not switching them both at\xa0 the same time, you might have version one of your back end serving a request for version two of\xa0 your front end. And then that API wouldn't exist, so there'd be errors visible to the user for\xa0 the duration of the deployment. This can be mitigated with complicated routing techniques,\xa0 but it's generally better to make API is backwards compatible. So make version two of the front end\xa0 be compatible with version one of the back end. So rolling deployments are relatively simple\xa0 to understand and generally well supported."}),"\n",(0,a.jsx)(t.p,{children:"If your users mind when there's\xa0 downtime, it's an excellent first step to deploy using a rolling deployment strategy.\xa0 The key programming consideration is to ensure that services can consume both the old version\xa0 and the new version of services API's. If this contract was violated, users might see errors\xa0 for the duration of the deployments. Let's talk more about deployment strategies.\xa0 And we'll go into bluegreen deployments. Another deployment strategy people\xa0 often see are bluegreen deployments."}),"\n",(0,a.jsx)(t.p,{children:"To set up a bluegreen. Deployment teams\xa0 need to disambiguate which services will be consistently deployed, and which services will\xa0 be shared across versions of the application. I'll explain a little bit more of what I mean by\xa0 that in the next section, a database server would be a shared resource, multiple versions of the\xa0 app would connect to the server at the same time, and a standard deployment would generally\xa0 not upgrade or modify the database. In our mern example, all of the\xa0 other services or cluster resources,"}),"\n",(0,a.jsx)(t.p,{children:"new versions of them would be\xa0 deployed on every prod push. So in a bluegreen deployment strategy, where\xa0 you're upgrading the JavaScript or the myrn app, this is what that would look like. So there's\xa0 a blue version, and a green version of the application where each is a fully standalone\xa0 stack. But we're each connects to a shared database, and the database is not part of blue\xa0 or green. It's a shared resource used by both bluegreen deployments are so called because they\xa0 maintain two separate clusters, one named blue and"}),"\n",(0,a.jsx)(t.p,{children:"one named green out of convention. If the current\xa0 version of the application is deployed to blue, we deploy the new version to green\xa0 and use it as a staging environment to ensure that the new version of the app\xa0 works correctly before setting users to it. After we're confident that the new\xa0 version of the software works correctly, we'd move over production load from blue to green, and then repeat the cycle in\xa0 the opposite direction. So here We started with the users being sent to blue,\xa0 which contains version one of the application. And"}),"\n",(0,a.jsx)(t.p,{children:"then we're investigating that version two works.\xa0 And after we're certain that version two works, will route users to version two. And\xa0 then the version one is unused. And we can shut it off and replace it with version three, make sure it works, and then switch user traffic\xa0 over to it over and over again. On the benefits side, bluegreen deployments are conceptually\xa0 very easy to understand. To set them up, you just have to create two identical production\xa0 environments and send requests to either one or"}),"\n",(0,a.jsx)(t.p,{children:"the other, which is relatively simple with\xa0 services like Amazon elastic load balancing. They're also quite powerful, longer running\xa0 tasks, like downloads can continue running in the old version of the application after\xa0 traffic is switched over to the new version. So if a user has an established\xa0 connection to green, and you've switched everyone else over to blue,\xa0 that connection can continue finishing whatever it was doing. So if you're watching video, and\xa0 you'd like to download it entirely to the user,"}),"\n",(0,a.jsx)(t.p,{children:"which might take minutes that can continue\xa0 going even during a prod push. Additionally, bluegreen deployments can be extended to many\xa0 different workflows, which we'll discuss. There's a few notable drawbacks to bluegreen\xa0 deployments, it's difficult to deploy a hotfix, for example, to revert a change, because the\xa0 old cluster might be running longer running tasks and unavailable to switch to. So if\xa0 you have version one of the application, you switch over to version two, you realize\xa0 version two is having problems, you might want to"}),"\n",(0,a.jsx)(t.p,{children:"push version three very quickly, which addresses\xa0 those problems. But version one would be the only place you could deploy version three. So you\xa0 wouldn't be able to do that. It's also finicky to transfer load between the clusters, if resources\xa0 autoscale, which we'll talk about later on, and load is transferred all at once the\xa0 new cluster might not have enough resources allocated to serve the surge of requests, because\xa0 requests went all at once to peak production load. And finally, if one cluster modifies the\xa0 shared service, like adding a column to a"}),"\n",(0,a.jsx)(t.p,{children:"table in the database, it may affect the other\xa0 cluster despite it not being the live one. So here's some common extensions to\xa0 bluegreen deployments. As I mentioned, they're very extensible. And many teams set up\xa0 advanced workflows around bluegreen deployments to improve stability and deployment velocity. The\xa0 first idea is a natural extension of bluegreen deployments, which I call rainbow deployments, but\xa0 I don't think there's a standard term for them. Instead of only having two clusters, some\xa0 teams keep an arbitrary number of clusters,"}),"\n",(0,a.jsx)(t.p,{children:"so blue, green, red, yellow, so on. This is useful\xa0 when you're running very long running tasks. If you're working on a distributed web scraper,\xa0 and you're scraping tasks take days, for example, you might need your clusters to\xa0 last until the last job is finished, to ensure things continue working as\xa0 expected. So with the rainbow deployments, you'd keep all of the clusters that are\xa0 still processing tasks around or if you're doing something like video encoding for long\xa0 videos, you don't want to shut off the cluster"}),"\n",(0,a.jsx)(t.p,{children:"that's in the middle of encoding a long video\xa0 because that work would have to be redone. In a regular deployment of clusters\xa0 that only be shut off after all of their long running jobs are done processing. Some teams rely heavily on manual QA and\xa0 don't use continuous deployment. They're often building desktop or mobile apps, which\xa0 needs to be published on longer release cycles. So if example.com is being routed to the blue\xa0 cluster, it would be relatively simple to deploy a new version of the application to the green\xa0 cluster endpoints new.example.com. to that. And"}),"\n",(0,a.jsx)(t.p,{children:"with this setup, the new version of the app could\xa0 be tested against the production database in the very environment that will soon become production.\xa0 Such tests are often called acceptance tests, because they're happening in production with\xa0 production data, with nothing like no privileged access to the code base. So for a game, you might\xa0 have the new release available, or the API's for the new release available and have your QA testers\xa0 test that. And after the QA testers give the Go"}),"\n",(0,a.jsx)(t.p,{children:"ahead, you can point the game client to the new\xa0 version, and then switch the labels of the to another useful add on to bluegreen deployments.\xa0 And really deployments in general is called the canary deployment. So if the new version of your\xa0 app contains subjective changes, such as editing the UI, it might be ill advised to push them\xa0 to all users at once. Facebook has billions of users. So if even 1% of their users complained\xa0 about a change, that would be overwhelming amounts of feedback. The changes may break users workflows\xa0 and need to be modified or rolled back in response"}),"\n",(0,a.jsx)(t.p,{children:"to user feedback. So in the context of a bluegreen\xa0 deployments, a canary deployment would be to an extension which routes maybe 5% of user traffic to\xa0 the new version of the application. And check that those users don't have negative feedback before\xa0 switching the rest of the users over. So if blue was version one green was version two, we'd have\xa0 95% going to version one. 5% going to version two We'd wait to see if anyone on version two\xa0 complained. If not, then we'd route everyone"}),"\n",(0,a.jsx)(t.p,{children:"to version two. And we'd shut off version\xa0 one, and then put version three in that one. So bluegreen deployments are powerful\xa0 and extensible deployment strategy that works well with teams that\xa0 are deploying a few times per day. With strategy only really starts being\xa0 problematic. It can use deployment scenarios, where there's many services being\xa0 deployed many times per day. Alright, let's keep talking about deployment.\xa0 And I'll see you in the next talk. continuous deployment can sound daunting, but\xa0 it's not actually as difficult as it might seem,"}),"\n",(0,a.jsx)(t.p,{children:"in many cases. Let's take a look back at\xa0 our current example, and how it's deployed. So, in our README, we've helpfully added this little line, which is\xa0 how we're currently deploying to production. If we look at our hosted version, slug, which is\xa0 hosted at this domain, we can see that the color is still purple, despite having changed\xa0 the color to blue in the previous video. The reason that it's still purple is\xa0 that we haven't production pushed, we haven't pushed the new version of\xa0 the code, which contains the blue color."}),"\n",(0,a.jsx)(t.p,{children:"And oftentimes, requiring human intervention\xa0 to deploy is simply unfeasible, especially as product skill. So to deploy, let's run\xa0 the deployment process manually first, and then let's talk about how to automate\xa0 it with a continuous deployment system. So here, we'll use a terminal and simply\xa0 run the command directly from the read. This developer computer comes with\xa0 the SSH key required to deploy otherwise it would be difficult to\xa0 disseminate this SSH key to all developers, which needed the ability to\xa0 deploy new versions of code."}),"\n",(0,a.jsx)(t.p,{children:"Here we can see that it's\xa0 using Docker compose rebuilt, we'll talk more about how to\xa0 set up a Docker file later on. Now, if we refresh the page, we can see that the deployment has\xa0 created a new version of the application, which is blue, so it's picked up the color\xa0 change which was merged in the previous commit. So continuous deployment, we'd like it to run on merges to the main branch, we don't\xa0 want to deploy a feature branches before they've been reviewed. For that,\xa0 we can set up a very simple configuration."}),"\n",(0,a.jsx)(t.p,{children:"We could write this configuration\xa0 file in any directory, but let's write it in the API directory for now. So we'll create another layer file will inherit from the testing layer file to make sure that the deployment\xa0 runs after tests have passed. And will only run the deployment\xa0 if the branch is the main branch. However, if it is the main branch,\xa0 we'd like to set up a secret and use that secret for our SSH key and\xa0 then use that SSH key to run the script. Let's do that now. And then hopefully gives us the\xa0 directives we need to expose the SSH key."}),"\n",(0,a.jsx)(t.p,{children:"What's appose? For now we're exposing the SSH key which is\xa0 used to authenticate with a production machine within the CI process itself. One other thing we need to do is change\xa0 the ownership to be more restrictive. This is required for SSH, but it might not\xa0 be required for other deployment processes. So now that we have our SSH key within the CI\xa0 server, and these steps are running after tests are passed, all we have to do is copy our command\xa0 and run it as if it was part of a CI process."}),"\n",(0,a.jsx)(t.p,{children:"All this configuration does is wait for tests to\xa0 pass, check that the branch is the main branch, and then use the SSH key to deploy\xa0 a new version of the application. Let's create a new pull request with\xa0 these changes and see how that looks. We can see that as before the ephemeral\xa0 environment and ci services have being built. But this API service is also being\xa0 built the API being the directory which contains our continuous deployment process.\xa0 Let's take a look at what the pipeline actually looks like Instructure."}),"\n",(0,a.jsx)(t.p,{children:"so here we can see that the application has been built successfully, and is being started, just as in their\xa0 regular ci without CD process. So we're running our continuous integration, but not our\xa0 continuous deployment step and this layer file. And we can see the tests are running. As usual, the test running process requires starting a fake browser.\xa0 So this takes around 30 seconds. And then after the tests pass, we'll see that the deployment process runs. So the\xa0 tests lead to the second level."}),"\n",(0,a.jsx)(t.p,{children:"graph, these are usually called\xa0 the build stages in a CI CD system. So here, we can see that the step was skipped because\xa0 the branch was not the main branch, which is exactly what we wanted. However,\xa0 now if we merge this pull request, we'll create a new merge branch on the main\xa0 branch. And we'll run the CI process once again. Here. And because this is the main branch, the deployment process itself will be running.\xa0 So let's take a look at what that looks like. We're simply loading the environment\xa0 to run the command and right now."}),"\n",(0,a.jsx)(t.p,{children:"And here, we can see that the deployment\xa0 is running within ci itself. So instead of needing to run it as an individual developer,\xa0 you can simply run this SSH command within ci. And this idea of deploying automatically from\xa0 a CI process is called continuous deployment. So let's work through that whole process\xa0 end to end once just to make sure that it's clear on the deployment automation side of things. So let's change the color, again, for the\xa0 main landing page, just to make sure that"}),"\n",(0,a.jsx)(t.p,{children:"it's visible if a change gets pushed correctly. And again, we'll change the two colors. And we'll create a new pull request. And now our reviewer will have a lot of\xa0 information about whether this change is good or not. So the reviewer will be\xa0 able to see both the files changed. So they'll see all we've\xa0 done is change a few colors, we'll be able to look at the CI process itself. So they'll be able to see that tests are running. So in particular, that the application\xa0 builds start successfully, tests run against,"}),"\n",(0,a.jsx)(t.p,{children:"they'll be able to look at\xa0 an ephemeral environment within minutes of me creating this new change. And if they approve the change, it'll be shown\xa0 to users in a short period of time, this whole process will only take about a minute, with the\xa0 longest part being these automated browser tests. One by one, these steps should become green. Again, this is the base. This is the\xa0 ephemeral environment. This is the continuous deployment process. And this is\xa0 the egg gets status. This is the one that"}),"\n",(0,a.jsx)(t.p,{children:"the administrators of GitHub might mark as\xa0 required. So that only if all of the checks pass, can the commit be merged and shown to users. so\xa0 here we can see that everything has passed. Let's take a look at the ephemeral environment just to\xa0 double check that the color is the one we want. We can see that from the thermal\xa0 environment, we change the color to be this rose ish red, perhaps this is the color\xa0 that was desired. So we'll say that this is correct. And the test has passed and\xa0 successfully posted a message. So we know"}),"\n",(0,a.jsx)(t.p,{children:"that the functionality of the application\xa0 has continued to work after this change. After we merge it be the end to end test deploy\xa0 process for for this merge commit. If we take a look at that, we'll see that because we\xa0 merged to the main branch the deployment is already running. in\xa0 production, we're creating a production build. And in a short period of time, the production\xa0 server should have the latest version of our application running on it. So here\xa0 it's restarting the production instance."}),"\n",(0,a.jsx)(t.p,{children:"And the snapshot is being taken. So everything is\xa0 succeeded, we've successfully pushed if we go to our website, it is now the shade of red that\xa0 we've changed. And that's what an end to end ci CD, ephemeral environment pipeline\xa0 generally looks like at a very high level. Alright, so let's talk more about\xa0 deployment automation in the next section, talked about deployment strategies. But that's\xa0 not the only thing in deployments. Deployment strategies help you reduce downtime and deploy\xa0 in a way that doesn't affect your users."}),"\n",(0,a.jsx)(t.p,{children:"But another key consideration for deployment is\xa0 making sure that there's enough resources for your containers or VMs. So that if there's a large\xa0 burst of users, your application doesn't go down. So let's say you're building a CI system, this hits close to home because I have\xa0 coursework at layer ci, a CI company, your users would push code, you'd\xa0 have to spin up runners to run tests against that code. And you'd see bursts of\xa0 traffic during the users business hours."}),"\n",(0,a.jsx)(t.p,{children:"And you'd see significantly less\xa0 traffic outside of those business hours. For a peak load of 10,000 concurrency runs,\xa0 you'd need at least 10,000 runners provisioned. However, at night, outside of peak hours, you wouldn't really need all 10,000\xa0 runners, most of them would sit idle. So your usage might look like this, which\xa0 is also indicative of a lot of applications, your lowest point is maybe 500. Runners required,\xa0 and your highest point is 10,000 runners required. So you need 20 times more workers from the highest\xa0 points in the day to the lowest points in the day."}),"\n",(0,a.jsx)(t.p,{children:"In an ideal world, you'd be able to create or\xa0 destroy these runners as necessary. during peak hours, you'd be able to create new ones, and then\xa0 off peak hours, you'd be able to destroy them. That's the idea for auto scaling. It's only possible to create and destroy\xa0 workers because of cloud providers. At their enormous scale, it's possible\xa0 to offer servers for cheap on small one hour leases. The most popular technology\xa0 at the time of this post or this video, is AWS easy to spot instances, which act exactly\xa0 like cloud hosted VMs with large discounts if"}),"\n",(0,a.jsx)(t.p,{children:"you provision them for short periods of time.\xa0 Another popular technology for auto scaling is Kubernetes horizontal pod auto scaling, which\xa0 sounds daunting. But since many providers provide Kubernetes out of the box, you can just assume\xa0 that if you're using Kubernetes, and containers, you'll get auto scaling if you configure\xa0 it correctly. Just to illustrate, if you're using Microsoft's as your as your cloud\xa0 provider, there's resources for auto scaling VMs. And for containers. If you're using AWS,\xa0 there's again, resources for VMs and containers."}),"\n",(0,a.jsx)(t.p,{children:"And if you're using Google Cloud, there's\xa0 resources for VMs and containers. auto scaling is usually discussed on the timeline\xa0 of one hour chunks of work. If you took the concept of auto scaling and took it to its limit,\xa0 you'd get serverless defined resources that are quickly started and use them on the timeline\xa0 of milliseconds. So one to 100 milliseconds. For example, a web server might not need to\xa0 exist at all until a visitor requests the page. Instead, it could be spun up specifically for that\xa0 request, serve the page and then shut back down."}),"\n",(0,a.jsx)(t.p,{children:"That's exactly the idea for serverless.\xa0 It's almost like taking auto scaling, so provisioning resources as the required and doing\xa0 it very quickly on very small time intervals. serverless is primarily used for services\xa0 that are somewhat fast to start in stateless, you wouldn't run something like a CI job\xa0 or a CI run within a serverless framework. But you might run something like a\xa0 web server or notification service. auto scaling is primarily used for services\xa0 that are slower to start or require state, you'd"}),"\n",(0,a.jsx)(t.p,{children:"likely run a CI job within an auto scaled VM or\xa0 container, and not within a serverless container. As a 2021, the distinction between the models is\xa0 becoming quite blurred. serverless containers are becoming popular. And they often run for upwards\xa0 of an hour. serverless containers act exactly like containers, but they're created and turned off in\xa0 a serverless manner. So in response to a trigger. Within a few years, it's likely\xa0 that serverless and auto scaling will converge into a single unified interface."}),"\n",(0,a.jsx)(t.p,{children:"So I'm excited about that. That's that's\xa0 going to be the future of deployment. And that ends our discussion of auto scaling\xa0 and serverless. I'll see you in the next talk. Another key concept in deployment\xa0 automation is service discovery. A database might be at one IP address, so 10 dot\xa0 1.1 dot 1.6 by four three chosen arbitrarily, while the web server would be at\xa0 another IP address 10.1 dot 1.2 8080. And they'd have to discover each other, because\xa0 the web server needs to talk to the database,"}),"\n",(0,a.jsx)(t.p,{children:"and the database might have\xa0 calls to the web server, those get even more complicated as you add more\xa0 copies of your web server, or add entirely new services. Again, let's consider the myrn app\xa0 from elsewhere in the academy DevOps series. So you have a web browser, the user themselves is\xa0 visiting your website. They're connecting to your front end, and they're connecting\xa0 to your backend making API calls. And your back end is connecting to a database. Here, there's three services that need to be\xa0 discovered. The browser needs to learn that"}),"\n",(0,a.jsx)(t.p,{children:"example.com corresponds to the front end,\xa0 an example.com slash API corresponds to the back end. And the back end needs to land on the\xa0 database is that 10.111 dot 1.3, for example. So the backend needs to know the IP address and\xa0 port of the database. And the browser needs to know the IP address and port of the backend and\xa0 the front end. In the very simplest configuration, everything is manually configured the back end\xa0 and front end or add static IPS. And given host names within DNS Domain Name System, it's the\xa0 mapping of example.com to an IP address on the"}),"\n",(0,a.jsx)(t.p,{children:"internet. And the back end is configured\xa0 to connect to MongoDB at a specific port. So your DNS configuration, this is the CloudFlare\xa0 configuration page, which has a DNS provider would look like this. So if the user visits\xa0 example.com, send them to this IP address. And if they visit api.example.com,\xa0 send them to that IP address. So this is all manually configured. And we've\xa0 just manually put the IP addresses in this. And then within the backend, we'd\xa0 read an environment variable, which is a dictionary of key value pairs that\xa0 are easily set when you're deploying things. So"}),"\n",(0,a.jsx)(t.p,{children:"you'd say connect to the environment\xa0 variable, specifying the MongoDB port, and then connect to Port 27017,\xa0 which is the default MongoDB port. And then when you're starting the backend, you just have to specify the IP\xa0 address that MongoDB is running in. This configuration is completely fine. For\xa0 simple products, it's difficult to mess up. It's relatively secure, and it doesn't over\xa0 complicate things, you can go pretty far with a simple configuration. Most products could launch\xa0 an MVP without any service discovery at all."}),"\n",(0,a.jsx)(t.p,{children:"But you'll know that you need to start caring\xa0 about complicating your service discovery when you see one of the following. So you need zero\xa0 downtime deployments. You can't hard code things like this if you want to do rolling deployments.\xa0 Because you can't easily automate where the arrows point to, you can't automatically change\xa0 the IP address. If you're doing such a simple deployment strategy. For example, if you have\xa0 more than a couple microservices, it's gonna get hard to remember where they all are. And if\xa0 you're deploying to several environments, like"}),"\n",(0,a.jsx)(t.p,{children:"if you have a developer environment, a staging\xa0 environment, and ephemeral environments, and production environments that all have different\xa0 IP addresses, it's gonna get pretty unwieldy to set the IP addresses all over the place. So let's\xa0 focus on zero downtime deployments because they're illustrative of the broader problem. Before\xa0 that, though, let's talk about reverse proxies, which are another crucial system design and\xa0 DevOps concept. The idea for a zero downtime deployment is simple. As we've seen, you start\xa0 a new version of the back end and front end, you"}),"\n",(0,a.jsx)(t.p,{children:"wait till they're up, and then you shut off the\xa0 old version of the backend and front end. So this happens in both rolling and bluegreen deployments.\xa0 However, it's difficult to update the IP addresses in DNS itself. If our rolling deployments\xa0 required changing these values directly in DNS, that wouldn't work very well, for various\xa0 reasons. And particular, DNS can take a long time to propagate users in other countries than\xa0 the United States, for example, might take days to see the new IP address, and they'd still\xa0 be trying to connect to the old version."}),"\n",(0,a.jsx)(t.p,{children:"The solution is to add a web server that acts\xa0 as the gateway to the front end and back end, we'd be able to change where it points to\xa0 without changing the DNS configuration itself. So web servers like these are called\xa0 reverse proxies. And they're really crucial for setting up zero downtime\xa0 deployments, and for service discovery itself. So taking our myrn app and adding this level\xa0 of complexity, the user's web browser would instead connect to the reverse proxy. So the user\xa0 would ask the DNS system where is example.com"}),"\n",(0,a.jsx)(t.p,{children:"The DNS system would respond with, oh, it's here, the IP address of the reverse proxy. And then\xa0 the reverse proxy would take the user's request and send it to the appropriate, they view\xa0 on that front end or back end, depending on what the user asked to connect to. And then\xa0 from there, everything else would be the same. So if you're running a deployments,\xa0 like a rolling deployments, the proxy could choose which of v1 or v2\xa0 of the front end or back end to send the users request to, and that would just\xa0 be by changing a configuration file."}),"\n",(0,a.jsx)(t.p,{children:"And then after your deployment is done, you\xa0 could turn off version one, and the reverse proxy could route traffic entirely diversion\xa0 to a straightforward approach is to store the service IPS in a hash table. So implicitly,\xa0 in the process of running at the moment, we assumed that our reverse proxy would be able\xa0 to know the IPS of the new versions of our apps, which is exactly the statements of service\xa0 discovery. And so that needing to manually tell our reverse proxy where the front\xa0 end and back end live. So where is the"}),"\n",(0,a.jsx)(t.p,{children:"IP address of version two of our back end, it\xa0 would be convenient if we could automate it. When the new versions come online, they can update the value for the key back end and\xa0 front end with their own IPS in this hash table. And then the reverse proxy could watch for changes\xa0 to the table and use that for routing decisions. For a very concrete example, which is about\xa0 as close as we're going to get to code in this set of videos, let's look\xa0 at this nginx configuration. nginx is a very popular reverse proxy. And it's\xa0 very commonly used in large tech companies."}),"\n",(0,a.jsx)(t.p,{children:"And it lets you define where various host names\xa0 go, if you pointed example.com, to the nginx, reverse proxy, again, in the picture, this\xa0 would be nginx, the user thinks they're connecting to your website, but they'd\xa0 be sending the request to nginx. Asking, for example, COMM And nginx would take their\xa0 example and forward it to your actual front end. So nginx just has to learn where\xa0 the IP for your front end is. And that's what this configuration would\xa0 do. So we're telling nginx directly,"}),"\n",(0,a.jsx)(t.p,{children:"take this key from this file, and then use conf D, which reads from a hash\xa0 table and updates the configuration file, and then send the user there. So all you need is a key value store, which has a\xa0 key fob where the front end IP is the current front end version. And then to run your rolling\xa0 deployments, you'd start a new front end version, check that it was alive, and then you just change\xa0 the key in the hash table to point to the new one. And then constantly would pick up that\xa0 change, replace this value with the new IP"}),"\n",(0,a.jsx)(t.p,{children:"of version two of the front\xa0 end, and then reload nginx, which would change the arrow to the\xa0 new version of the front end like this. That was a lot to deal with. So let's back up a bit, all you'd need\xa0 to do is update your front end, to set the key in the hash table for\xa0 IPS front end to be the front ends IP, and then make your back end do the\xa0 same for the back end location. And that way, when the new version of the\xa0 application starts, it would update the key in the table. And then nginx would start routing\xa0 users to the new version of the application."}),"\n",(0,a.jsx)(t.p,{children:"This is what proxy passing means and nginx. So you see this proxy pass directive.\xa0 But this is all very complicated. It's just an illustrative point of if you were\xa0 to implement this yourself, how would you do it? The most common thing used in industry\xa0 is service discovery by using DNS itself. So DNS, we thought of before as the\xa0 slow protocol that might take days to propagate changes across the\xa0 network. But you can run DNS locally. And that's the industry standard. So let's talk about DNS a little bit."}),"\n",(0,a.jsx)(t.p,{children:"The idea for DNS is just to map host names to\xa0 IPS. When you visit layer ci.com. For example, the global DNS system will first map\xa0 the name latest.com, to the addresses at the time of this video 104 dot 217 9.86 and\xa0 172 dot six 7.16 9.106, which are just arbitrary computers connected to the internet. And you\xa0 can use the Digg command on a website to see where those addresses are. So this is saying for\xa0 the key layer ci.com the values are these two. And usually when people mentioned\xa0 DNS, they mean the global service."}),"\n",(0,a.jsx)(t.p,{children:"So visiting websites on the internet. However, as\xa0 I mentioned, it's possible to run DNS internally. It would be ideal if in our nginx configuration,\xa0 we could specify HTTP full colon slash slash front end and then have front end resolve\xa0 to the IP of our front end service. That way we wouldn't have to change\xa0 anything except for the DNS configuration. That's exactly how DNS based\xa0 service discovery works. you configure your services to query a server\xa0 you control for DNS queries and then this"}),"\n",(0,a.jsx)(t.p,{children:"So instead of saying MongoDB, full colon\xa0 slash slash, process and MongoDB, you just say MongoDB full colon slash slash\xa0 Mongo, where Mongo is a key in the key value pairs in\xa0 the DNS that you control. Of course, it's not trivial to deploy\xa0 your own DNS server. In practice, though there are popular options like core DNS, the most likely thing you do is use a cloud\xa0 provider or Kubernetes internal solutions. So the end result you'd\xa0 get is something like this, the user's web browser would connect to nginx.\xa0 Thinking it was the website nginx would ask"}),"\n",(0,a.jsx)(t.p,{children:"the DNS provider, where is the API right\xa0 now, the cloud provider would respond with this is the IP address, given the deployments,\xa0 so blue, green, or rolling deployments, this is what we currently want users to visit\xa0 when they want to visit the API. And then this would correspond to version\xa0 one or version two of the backend. And then the proxy would\xa0 forward the request there, the request to be fulfilled, and then we go\xa0 back to the proxy, and then back to the user. So the conclusion of all of this is that service\xa0 discovery is tricky, but vitally important as a"}),"\n",(0,a.jsx)(t.p,{children:"foundational building block for these deployment\xa0 strategies. And for deployment automation in general, if you configure service discovery\xa0 in an appropriate manner for your deployments, so DNS based in a Kubernetes cluster, for example,\xa0 it makes significantly easier for developers to have microservices that talk to each other,\xa0 instead of having a developer have to write connect a MongoDB app, and then deal with where\xa0 MongoDB actually is. They can simply say connect to MongoDB at MongoDB colon slash slash Mongo.\xa0 And then you as the DevOps platform engineer can"}),"\n",(0,a.jsx)(t.p,{children:"configure where that Mongo always points to the\xa0 right place the right IP address. By decoupling the application logic from the deployment\xa0 logic, you'll help the developers on your team build faster, and you'll be able to deploy\xa0 it more easily. So that's it for deployments. Let's go on to the next and final pillar,\xa0 which is application performance management. There aren't that many general topics\xa0 in application performance management, so this section will be a little bit shorter.\xa0 We'll go into more detail in future sections"}),"\n",(0,a.jsx)(t.p,{children:"in the DevOps Academy. But just\xa0 for this introductory video series, let's talk about two core concepts.\xa0 The first of which is log aggregation. And it's a way of collecting and tagging\xa0 application logs from many different services into a single dashboard that can easily be\xa0 searched. One of the first systems that have to be built out in an application performance management\xa0 system is log aggregation. Just as a reminder, application performance management is the part\xa0 of the DevOps lifecycle where things have been"}),"\n",(0,a.jsx)(t.p,{children:"built and deployed. And you need to make\xa0 sure that they're continuously working so they have enough resources allocated to\xa0 them. And errors aren't being shown to users. In most production deployments, there are many\xa0 related events that emit logs across services. At Google, a single search might hit five different\xa0 services before being returned to the user. If you got unexpected search results, that might mean\xa0 a logic problem in any of the five services. And log aggregation helps companies like\xa0 Google diagnose problems in production,"}),"\n",(0,a.jsx)(t.p,{children:"they built a single dashboard where they can\xa0 map every request unique ID. So if you search something, your search will get a unique ID\xa0 and then every time that search is passing through a different service, that service will\xa0 connect that ID to what they're currently doing. This is the essence of a good\xa0 log aggregation platform, efficiently collect logs from everywhere that\xa0 emits them and make them easily searchable. In the case of a fault. Again, this is our\xa0 main app, the users web browser connects to"}),"\n",(0,a.jsx)(t.p,{children:"a back end and the front end, and the\xa0 back end then connects to a database. If the user told us, the page turned\xa0 all white and printed an error message, we would be hard pressed to diagnose\xa0 the problem with our current stack, the user would need to manually send us the\xa0 error and we'd need to match it with relevant logs in the other three services. Let's\xa0 take a look at Elk, a popular open source log aggregation stack named after its three\xa0 components, Elasticsearch, LogStash and cabana."}),"\n",(0,a.jsx)(t.p,{children:"If we installed it in our burn app, we'd get three\xa0 new services. So the users web browser, again would connect to our front end and back end. The\xa0 back end would connect to Mongo, and all of these services, the browser, the front end, the back\xa0 end and Mongo would all send logs to LogStash. And then the way that these three components work, the components of ALK Elasticsearch Log\xa0 Stash and cabana is that all of the others Services send logs to LogStash. LogStash\xa0 takes these logs, which are text emitted by"}),"\n",(0,a.jsx)(t.p,{children:"the application. For example, the web browser.\xa0 When you visit a web page, the web page might log this visitor access this page at this\xa0 time. And that's an example of a log message. Those logs would be sent to LogStash, which would\xa0 extract things from them. So for that log message, user did thing a time, it would extract the time\xa0 and extract the message and extract the user and include those all as tags. So the message\xa0 would be an object of tags and message so that you could search them easily. You could say,\xa0 find all of the requests made by a specific user."}),"\n",(0,a.jsx)(t.p,{children:"But LogStash doesn't store things itself.\xa0 It stores things in Elasticsearch, which is efficient database for querying text.\xa0 And Elastic Search exposes the results as Kibana and cabana is a web server that connects\xa0 to Elasticsearch and allows administrators as the DevOps person or other people\xa0 on your team, the on call engineer to view the logs in production\xa0 whenever there's a major fault. So you as the administrator would connect to\xa0 cabana cabana would query Elastic Search for logs matching whatever you wanted. You could say,\xa0 hey, cabana, in the search bar, I want to find"}),"\n",(0,a.jsx)(t.p,{children:"errors. And cabana would say Elastic Search\xa0 finds the messages which contain the string error. And then Elasticsearch would return\xa0 results that had been populated by LogStash and LogStash would have been samples\xa0 results from all of the other services. If you visited a web page, this might\xa0 be the sort of log that is emitted. And it might be processed into an object like\xa0 this. So it has a format, a date, and a simple time format. That's the same for all messages\xa0 emitted by all different services, you'd have a"}),"\n",(0,a.jsx)(t.p,{children:"service which service submitted the log, and you'd\xa0 have the message, the actual content of the log. And the processor, LogStash itself would often\xa0 be connected to the internet so that JavaScript in the browser can catch errors and send them to\xa0 LogStash. Although there are additional services like century that might be better suited\xa0 for that. How would we use elk to diagnose a production problem? Well, let's say a user says\xa0 I saw error code 1234567. When I tried to do this, with elk setup, we'd have to go to cabana, enter\xa0 1234567 in the search bar, press Enter. And then"}),"\n",(0,a.jsx)(t.p,{children:"that would show us the logs that corresponded\xa0 to that. And one of the logs might say, internal server error returning 1234567. And we'd see that\xa0 the service that emitted that log was back end, and we'd see what time that blog was\xa0 emitted at. So we could go to the time in that log. And we could look\xa0 at the messages above and below it in the backend. And then we could see a better\xa0 picture of what happened for the user's request. And we'd be able to repeat this process\xa0 going to other services until we found"}),"\n",(0,a.jsx)(t.p,{children:"what actually caused the problem for the user. The final piece of the puzzle is ensuring\xa0 that logs are only visible to administrators. As logs can contain sensitive information like\xa0 tokens, it's important that only authenticated users can access them. You wouldn't want to\xa0 expose Kibana to the internet without some way of authenticating. My favorite way of doing\xa0 this is to add a reverse proxy like nginx. Again, our friend nginx and then have the auth request\xa0 mechanism check that the user is logged in."}),"\n",(0,a.jsx)(t.p,{children:"So in our back end, we could\xa0 add something like this, which simply returns a successful status. If\xa0 the user visits example.com slash auth request and there an admin, it would return a successful\xa0 status. And if they're not admin, it would return an unauthorized status. And then we could\xa0 configure nginx. Again, as mentioned in previous videos, to have these location blocks, the slash\xa0 private location would connect to slash off. And then we could make sure that if this was slash\xa0 logs, for example, that the user was logged in,"}),"\n",(0,a.jsx)(t.p,{children:"because with this auth request directive, if\xa0 the user visits slash logs, and they're not an administrator, they wouldn't be able to access\xa0 them. Alternatively, Elasticsearch itself is run by a company called elastic and they have a paid\xa0 version, which contains something called x pack, which facilitates this as well. So you can go for\xa0 either a reverse proxy which authenticates users, or the paid version of the application. As an aside, you can use log\xa0 aggregation as an extra test. So in your ci pipelines, where you\xa0 want to tell if code is good or not,"}),"\n",(0,a.jsx)(t.p,{children:"you can repurpose your log aggregation stack\xa0 to ensure that no warnings or errors occur while the tests run. If your end to end test\xa0 looks like this. So you're starting your stack, you're starting your logging stack. You're\xa0 running your tests with NPM run test. You could add an extra step which queries Elastic Search for\xa0 logs matching error, and you could make sure that There are no logs, that printed error.\xa0 And then even if all of your tests pass, if there's an error going on, that error might\xa0 be important despite all the tests passing."}),"\n",(0,a.jsx)(t.p,{children:"So this adds a free extra check to your ci stack. And there's a few examples of log aggregation\xa0 platforms. There's Elasticsearch, LogStash, Kibana, which we talked about, there's fluent D\xa0 is another popular open source choice. There's data dog, which is very commonly used at larger\xa0 enterprises. It's a hosted offering. And there's log DNA, which is another hosted offering.\xa0 And those cloud providers also provide logging facilities like AWS, cloudwatch logs. So log\xa0 aggregation is a key tool for diagnosing problems"}),"\n",(0,a.jsx)(t.p,{children:"in production. It's relatively simple to install\xa0 a turnkey solution like elk or cloud watch, and it makes diagnosing and triage problems\xa0 and production significantly easier. That's it for log aggregation.\xa0 I'll see you in the next talk. Plus topic we're going to talk\xa0 about is metric aggregation. metrics are simply data points that tell\xa0 you how healthy production is. So as you can see on the screen, things like CPU usage,\xa0 memory usage, disk IO, file, system fullness, are all important production metrics that you\xa0 might care about. If log aggregation is the"}),"\n",(0,a.jsx)(t.p,{children:"first tool to set up for production monitoring,\xa0 metrics, monitoring would be the second. They're both indispensable for finding production faults,\xa0 and debugging performance and stability problems. Log aggregation primarily deals with text,\xa0 logs or textual Of course. In contrast, metric aggregation deals with numbers. How\xa0 long did something take his memory being used? It's frighteningly difficult to understand\xa0 what's going on in a production system. Netflix, for example, measures\xa0 2.5 billion different time series"}),"\n",(0,a.jsx)(t.p,{children:"to monitor the health of their production\xa0 deployments. Successful metric monitoring is being able to automatically notify the necessary\xa0 teams when something goes wrong in production. Let's keep looking at open source implementations\xa0 of DevOps tools to keep things general. Prometheus is a tool originally deployed at\xa0 SoundCloud is one of the most popular metrics servers. And this is what it looks like. Similarly\xa0 structured, the inputs are sent to the retrieval, things like nodes would send how much disk usage\xa0 they have to the media server, but also how"}),"\n",(0,a.jsx)(t.p,{children:"long services are taking ALC itself would parse\xa0 numbers out of logs and send them to permit this. And then promethease figures out what get services\xa0 from using service discovery from the previous video. And then it takes those and it stores it\xa0 in a time series database equivalent for numbers, what Elasticsearch is for text, and then that\xa0 stored on the Prometheus server node itself. And then finally, there's a front end. So other\xa0 services can query promethease. To do things, one thing you might want to do is if there's something\xa0 terribly wrong, like your website is down,"}),"\n",(0,a.jsx)(t.p,{children:"you might want to connect a pager duty\xa0 or email someone or send someone a text message with Twilio, beyond call engineer\xa0 and tell them that something is wrong. But you might also want to query\xa0 metrics to get a view like this one. And that's what prom qL is used for. So grafana\xa0 is the view that dark view with the graphs. And it's common way of viewing these\xa0 time series. But you can make your own and you can make API's. And there's many\xa0 other front ends that connected previous."}),"\n",(0,a.jsx)(t.p,{children:"The diagram above is daunting, but it's quite\xa0 similar to the architecture that we discussed for log aggregation frameworks. There's four key\xa0 components. Like I mentioned, the time series database actually stores the measurements,\xa0 retrieval, the alert manager and the web UI. So the sorts of metrics we collect. Well,\xa0 there's a lot of subjectivity about which metrics are important based on what your\xa0 product does and what your users are. But here's a few ideas for what you\xa0 store in something like promethease."}),"\n",(0,a.jsx)(t.p,{children:"So request fulfillment times, these are very\xa0 useful for understanding when systems are getting overloaded, or if a newly pushed\xa0 change has negatively impact performance. The format times are often parsed out of\xa0 logs using a regular expression, for example, or taken out of a field in a database. For a website or REST API, a common request\xa0 fulfillment time would be time to response for web sites and rest API's. A common request\xa0 fulfillment time would be time to response. That way slow web pages could be\xa0 discovered and identified in production."}),"\n",(0,a.jsx)(t.p,{children:"A related metric that is very indicative of\xa0 problems his request counts, and if there's a huge spike in requests per second, it's\xa0 very likely that at least a few production systems will have trouble scaling. Watching\xa0 request counts can also be used to detect and mitigate attacks like denial of service attacks, which are when attackers sent many malicious\xa0 requests to services in production. The last common metric across many\xa0 types of companies is server resources. Here's a few examples. So the database size\xa0 and maximum database size. If you have two"}),"\n",(0,a.jsx)(t.p,{children:"terabytes of disk for your database and your 1.5\xa0 terabytes in, you might want to alert someone to increase the amount of disk available for the\xa0 database or delete things that are being unused web server memory. So if your web server\xa0 is taking a lot of requests per second and doing a lot of processing, it might require\xa0 more memory. So if it runs out of memory, it would crash and your users wouldn't\xa0 be able to access your website anymore. Network throughput. So if you're downloading\xa0 many things, or uploading many things,"}),"\n",(0,a.jsx)(t.p,{children:"you can saturate your network. And that\xa0 would also cause degraded performance. And a final one is TLS certificate expiry\xa0 time. So this lock in the browser that uses TLS certificates to see whether the browser\xa0 is secure or not, are used all over the place internally. And these cause problems if they're\xa0 not measured, and they're not alerted for. So for example, Google Voice had an outage in\xa0 2021, Google of all companies wasn't measuring when their TLS certificates would expire.\xa0 And that caused an outage a few months ago."}),"\n",(0,a.jsx)(t.p,{children:"So production faults very rarely look\xa0 like no users can access anything. There are often a gradual ramp,\xa0 certain API's taking longer and longer, and then eventually everything breaks. portal\xa0 analysis is an easy way to pare down production statistics into something actionable. A website\xa0 might measure how long it takes for websites to fully load their landing page to notice\xa0 when there's a very obvious production issue. So with cuartel analysis, you'd split\xa0 request times and many different buckets."}),"\n",(0,a.jsx)(t.p,{children:"How long did the slowest 1% of users take?\xa0 How long did the slowest 5% of users take? How long did the slowest 25% of users take.\xa0 So if your landing page is slower when users are logged in and logged out, just by visiting\xa0 without a logged in user, you might not notice that the web page is very slow. But users\xa0 that are logged in, would show up in the 1% of requests bucket, and you'd see that those\xa0 users are having a bad degraded experience. Or when example stackoverflow.com itself was\xa0 notified of an outage because their landing"}),"\n",(0,a.jsx)(t.p,{children:"page was taking a long time to respond\xa0 to requests due to a specific post that was published to stack overflow. For metrics\xa0 analysis, there's many common production tools. There's Prometheus and grafana. As we\xa0 mentioned, there's data dog, again, not only log aggregation, but metrics aggregation\xa0 as well. There's New Relic, which is I would say maybe the old reliable option. And there's\xa0 again, cloud providers by their own versions of this. There's AWS cloudwatch metrics, Google\xa0 Cloud monitoring, and as your monitor metrics."}),"\n",(0,a.jsx)(t.p,{children:"That's it for application performance\xa0 management. Thanks for watching."})]})}function c(e={}){const{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,a.jsx)(t,{...e,children:(0,a.jsx)(d,{...e})}):d(e)}},8453(e,t,o){o.d(t,{R:()=>i,x:()=>r});var n=o(6540);const a={},s=n.createContext(a);function i(e){const t=n.useContext(s);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:i(e.components),n.createElement(s.Provider,{value:t},e.children)}}}]);